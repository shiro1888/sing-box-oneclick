# 节点搭建文档

这是一份通用节点搭建文档，不绑定某一台 VPS。以后新买 VPS、换 IP、换域名，都按这份模板改变量即可。

核心目标：

```text
一台自用 VPS
四条协议节点（Hysteria2 / AnyTLS / Vless / SS2022）+ 可选第 5 条 CF-Vless 大保底
一个 Clash/Mihomo 订阅
一个手动选择代理组
国内直连，国外代理
客户端显示订阅流量（有限流量可限流停机，无限流量只显示不停）
配置简单，可排查，可迁移
```

> 本版本相对早期版本做了若干健壮性优化：流量统计支持双向计费、运行参数统一到单一来源 `/etc/sing-box-node.env`、手动停机不会被定时任务拉起、补齐防火墙/时间同步/备份等步骤。改动原因见文末「之前改动抽象成通用规则」。
>
> 2026-07-26 修订：变量表/运行参数前置；新增「两条路线怎么选」；单机实测记录归位到文末「实例记录」；修复 traffic_limit.py 账期匹配、ufw 默认 UDP 端口、env heredoc 变量展开三处实际问题；统一 SS2022 UDP 默认口径；合并 CF-Vless 死节点等重复警告。

## 使用方式

如果是手机 Clash/Mihomo 导入节点，只需要订阅 URL（`SUB_PATH` 已自带开头 `/`，所以这里不再多写斜杠）：

```text
http://SUB_DOMAIN_1SUB_PATH
```

客户端里的订阅名称建议填机场 / 线路名字：

```text
AIRPORT_NAME
```

例如这条线路叫 `LINE-01`，客户端订阅名称就填 `LINE-01`。以后如果有多条线路，可以用 `US-01`、`JP-01`、`HK-01` 这种名字区分。

如果是新 VPS 部署，从“变量表”开始往下看。

如果是维护旧 VPS，先看“检查命令”和“修改前备份”。

如果只是想知道最终原则，看这一段：

```text
订阅用随机路径 + 流量头只在订阅路径输出；首页、旧路径、错误 Host 一律 404
代理组用 select 手动切换；节点名只保留协议名（Hysteria2 / AnyTLS / Vless / SS2022）
443 留给 VLESS Reality；订阅默认走 80，要加密再按「订阅传输安全」升级
运行参数集中在 /etc/sing-box-node.env；流量默认按 tx 计；手动停机不会被定时任务拉起
Google Play / GMS 下载链路高优先级走代理，放在国内分流前面
Cloudflare / Argo / WARP 只做保底不做主线；订阅域名和 CF 保底域名分开
看板页含节点凭证，随机路径不是登录保护，装完立即 panel-pass 加密码
低配 / 甲骨文机器走轻量路线：少常驻服务、日志 warn、不装公网面板、不拿 UDP 压测
```

完整规则和每一条背后的原因，见文末「之前改动抽象成通用规则」表。

## 两条路线怎么选（一键脚本 vs 手搓）

本文档和一键脚本是同一套架构的两种部署方式，先选路线再往下读：

| 路线 | 适合场景 | 怎么用 |
|---|---|---|
| **一键脚本（快速开新机推荐）** | 快速部署、复刻同一套架构 | 仓库 `github.com/shiro1888/sing-box-oneclick`，按 README 跑 `install.sh`；装完立刻 `install.sh panel-pass "$(openssl rand -hex 12)"` 给看板加密码 |
| **手搓（本文档主线）** | 想理解每一步、定制、排障 | 从下面「变量表」开始顺读照做 |

一键脚本常用子命令速查（完整列表以仓库 README 为准；功能边界见「一键脚本和面板的取舍」「可选增强」两节）：

```text
install.sh              主流程：四协议 + 订阅 + 流量显示/限流
install.sh panel        生成可视化看板页        panel-pass   给看板加登录密码
install.sh cf           CF-Vless-WS 大保底一键版  warp        WARP 解锁分流
install.sh set          改 LIMIT_GB / EXPIRE_AT 等运行参数
install.sh doctor       一键自检                harden       SSH 加固（带防锁死护栏）
install.sh backup / restore    备份 / 换机迁移
```

两条路线的边界：正文里凡是出现 `install.sh xxx` 的段落都属于脚本路线；纯手搓读者照正文命令做即可，遇到 `PANEL_PATH` / `$sb_ok` 这类只有脚本才会生成的产物，跳过或删除对应配置段。反过来，一键脚本用户不要手改脚本管理的文件（`/etc/sing-box-node.env`、看板页等），要改用 `install.sh set` / `install.sh panel`，避免手改和脚本互相覆盖。

### 一键脚本的事务与回滚边界（2026-07-31）

当前脚本对修改型动作使用同一把维护锁。`install`、`set`、`panel`、`panel-pass`、`restore`、`cf`、`warp`、`update`、`restart`、`uninstall` 等不能并发运行，避免两个动作都基于旧快照，最后完成者把先完成者的状态覆盖掉。菜单里的动作也在独立的 `errexit` 子 shell 中运行，内部命令失败不会继续执行后续覆盖步骤。

事务范围：

```text
install 后半段：密钥、运行参数、证书、sing-box 配置、订阅/看板、Nginx、流量脚本和端口跳跃
set：env、sing-box 配置、订阅/看板和流量头
panel / panel-pass：看板、登录页、密码 map 和 Nginx 配置
cf：cloudflared 服务、sing-box CF 入站、cf.env、待重接状态、订阅和看板
warp：sing-box 配置和 warp.env
```

静态文件不是边生成边覆盖：Clash YAML、Base64、看板和登录页会先全部渲染、校验，再作为文件组提交。提交中途失败会恢复整组旧文件，避免出现 YAML 已更新、Base64/看板仍旧的混合状态。密钥、env、CF/WARP 状态也使用同目录临时文件后原子替换。

回滚原则：

```text
失败先恢复旧文件，再重启/重载旧服务并确认 active。
自动回滚完全成功才删除临时备份。
回滚自身失败时必须保留备份目录并打印路径，不得继续误报“旧节点不受影响”。
依赖包安装、云安全组和已有 ufw 放行属于外部或幂等副作用，不会因后半段失败自动卸载/收回。
```

跨机 `restore` 的 CF 状态是特殊情况：备份中的 `cf.env` 会先转成 `/etc/sing-box/cf.restore-pending.env`。新机没有 Connector token 时，不发布旧 CF 节点；运行 `CF_TOKEN='新机 Connector token' bash install.sh cf`，本机和公网都拿到严格 HTTP `101` 后才转为正式 `cf.env` 并进入订阅。显式传入的 CF 域名、端口、UUID、WS 路径和显示名优先于旧状态。

看板密码额外限制：只允许至少 6 位的小写字母、数字、`.`、`_`、`~`、`-`，但不能以 `~` 开头，也不能使用 `default`、`hostnames`、`volatile`、`include` 等 Nginx `map` 保留字。推荐直接用 `openssl rand -hex 12`。

## 变量表

部署前先确定这些值。分成两类。

> 占位符替换规则：正文所有大写 token 用真实值做整篇查找替换。注意 `SUB_PATH` 自带开头的 `/`，所以正文里会出现 `http://SUB_DOMAIN_1SUB_PATH`、`/var/www/htmlSUB_PATH` 这种直接拼接的写法——替换后就是正常路径（例如 `/var/www/html/sub-a8f3c91d7e4b4c6f.yaml`），不是笔误。

### A. 必填占位符（主线必填，IP 类按机器 IP 栈调整）

文档正文里凡是出现这些大写 token，都用真实值做整篇查找替换。其中 `SERVER_IP` / `SERVER_IPV6` / `SERVER_ADDRESS` 按 IPv4-only / IPv6-only / 双栈分情况填（见「IPv4 / IPv6 / 双栈」一节），不是每台都三个全填。

| 变量 | 说明 | 示例 |
|---|---|---|
| `AIRPORT_NAME` | 机场 / 线路在客户端里的订阅名称 | `LINE-01` |
| `SERVER_IP` | VPS 公网 IPv4（IPv4 / 双栈机器的 `IP-CIDR/32` 分流规则引用它；纯 IPv6 改用 `IP-CIDR6,SERVER_IPV6/128`） | `1.2.3.4` |
| `SERVER_ADDRESS` | 节点 `server:` 实际连接地址。**IPv4 默认填 `SERVER_IP` 的值**；纯 IPv6 填域名 `SUB_DOMAIN_1` | `1.2.3.4` |
| `SERVER_IPV6` | VPS 公网 IPv6，仅 IPv6 / 双栈用到（分流规则的 `IP-CIDR6/128` 引用它） | `2001:db8::1` |
| `SUB_DOMAIN_1` | 主订阅域名 | `node.example.com` |
| `SUB_DOMAIN_2` | 备用订阅域名，没有就删掉相关规则 | `node2.example.com` |
| `SUB_PATH` | 随机订阅路径 | `/sub-a8f3c91d7e4b4c6f.yaml` |
| `HY2_PASSWORD` | Hysteria2 密码 | `openssl rand -hex 16` |
| `ANYTLS_PASSWORD` | AnyTLS 密码 | `openssl rand -hex 16`（与上面不同的一条） |
| `VLESS_UUID` | VLESS UUID | `sing-box generate uuid` |
| `REALITY_PRIVATE_KEY` | Reality 私钥，服务端用 | `sing-box generate reality-keypair` |
| `REALITY_PUBLIC_KEY` | Reality 公钥，客户端用 | 同上命令的 PublicKey |
| `REALITY_SHORT_ID` | Reality short-id | `openssl rand -hex 8` |
| `SS_PASSWORD` | Shadowsocks-2022 密码（base64，**不是 hex**） | `openssl rand -base64 16`（128-gcm 用 16 字节；256/chacha 用 32） |

运行参数（流量/到期/网卡/计费方式）不在这里手填，统一写进 `/etc/sing-box-node.env`，见“运行参数：单一来源”一节。

### B. 推荐默认值（一般保留，要改需多处同步）

这些值模板里直接写成了推荐默认，不是查找替换的占位符。保留即可；**如果要改，下面每个位置都要同步改**，否则服务端和客户端会对不上。

| 变量 | 默认值 | 出现位置 |
|---|---|---|
| `HY2_PORT` | `4433/udp` | sing-box 入站、Clash 节点、防火墙、端口检查 |
| `ANYTLS_PORT` | `4434/tcp` | sing-box 入站、Clash 节点、防火墙、端口检查 |
| `VLESS_PORT` | `443/tcp` | sing-box 入站、Clash 节点、防火墙、端口检查 |
| `SS_PORT` | `4435/tcp+udp` | sing-box 入站、Clash 节点、防火墙、端口检查 |
| `SS_METHOD` | `2022-blake3-aes-128-gcm` | sing-box 入站 `method`、Clash 的 `cipher` |
| `REALITY_SNI` | `www.bing.com` | sing-box 的 `server_name` 和 `handshake.server`、Clash 的 `servername` |
| `TLS_SNI` | `www.bing.com` | 自签证书 CN、Clash 的 `sni` |

> 注意：`REALITY_SNI` 必须在「服务端 `handshake.server`」和「客户端 `servername`」两处完全一致，否则 Vless 节点会静默失败（连得上 TLS 但代理不通，很难排查）。改 SNI 时务必两处一起改。
> 还要确认这个 SNI/握手域名能从 VPS 自己稳定访问。Reality 服务端握手时要去连这个伪装目标；如果目标站偶发超时，客户端会表现为 Vless Timeout / REALITY authentication failed。不要迷信大厂域名，部署时用 `curl -4 --connect-timeout 3 https://REALITY_SNI/` 连续测几次，选稳定的。

## 运行参数：单一来源

流量额度、到期时间、统计网卡、计费方式统一放在一个文件，脚本和初始化都从这里读，避免“改了一处忘了另一处”。

> 建议：不管走哪条路线，先 `export LIMIT_GB` 和 `EXPIRE_AT` 再执行——下面的 heredoc 用 `${VAR:-默认}` 写法，会优先采用你导出的值；一键脚本路径则避免默认回退到写死的 `+365 days`。
> 例：`export EXPIRE_AT="$(date -d '+30 days' '+%Y-%m-%d %H:%M:%S %z')"` （按你的真实续费日改成对应日期）

```bash
# 先用 IPv4 默认路由探测出口网卡；纯 IPv6 机器 IPv4 探测会失败，再用 IPv6 兜底。
# 两者都失败才回退 eth0（网卡名为 ens3/enp1s0 等时，这一步能避免限流脚本首次报 interface not found）。
DETECTED_IF=$(ip route get 1.1.1.1 2>/dev/null | grep -oP 'dev \K\S+' | head -1)
[ -z "$DETECTED_IF" ] && DETECTED_IF=$(ip -6 route get 2606:4700:4700::1111 2>/dev/null | grep -oP 'dev \K\S+' | head -1)

cat >/etc/sing-box-node.env <<EOF
# 每月显示/限流额度。变量名 LIMIT_GB 与一键脚本保持一致，但单位实际是 GiB（脚本按 1024^3 字节算，不是十进制 GB）。
# 商家如果按十进制 GB 标流量，这里要换算：例如商家 200GB ≈ 186 GiB，填 186 才不会晚停约 7.4%。
# 无限流量的机器：把它设得很大（如 999999），脚本就只显示用量、永不停机（见「部署前须知 - 流量限制」）。
# 下面的 ${VAR:-默认} 会优先用你 export 过的同名变量，没导出才落到默认值。
LIMIT_GB=${LIMIT_GB:-200}
# VPS / 订阅到期时间（带时区偏移，格式必须是 %Y-%m-%d %H:%M:%S %z）
# 注意：EXPIRE_AT 只用于客户端显示到期，限流脚本不会在到期时停服务（只有 LIMIT_GB 超额才真停）。
# 要到期自动停机，自己加一条 cron/at，或在脚本里加 now>expire 时 systemctl stop sing-box。
# 建议：不要写死固定时间。请按你实际续费日填写，不要默认用“安装日 + 365 天”。
# 常用示例：按本机1个月后到期（按你自己的续费日替换；\$ 转义是故意的，保证注释原样落盘）：
# EXPIRE_AT="\$(date -d '+30 days' '+%Y-%m-%d %H:%M:%S %z')"
EXPIRE_AT="${EXPIRE_AT:-}"
# 如果你先临时不展示到期，可先留空；脚本会回退 expire=0（客户端通常视为“无到期”）。
# 统计流量的网卡（自动探测，探测失败回退 eth0，可手动改）
INTERFACE=${DETECTED_IF:-eth0}
# 计费方式：tx=只算出站(默认,匹配绝大多数商家) / max=取较大方向 / rx+tx=双向相加(仅商家真按进+出计费才用)
COUNT_MODE=tx
EOF

chmod 600 /etc/sing-box-node.env
cat /etc/sing-box-node.env
```

`COUNT_MODE` 怎么选（默认 `tx`，对绝大多数机器是对的）：

```text
tx     只算出站（默认）。Vultr / DigitalOcean / AWS / GCP / Hetzner 等绝大多数商家
       都只按出站(outbound)计费——这个值正好等于你的真实用量，最贴合账单。
max    取进/出较大的一方。代理 rx≈tx，所以 max≈tx，比 tx 多一丁点安全余量。
rx+tx  把进+出相加。只有极少数商家真的按"进+出双向"计费，才用它。
```

> **为什么默认不是 rx+tx（重要纠正）**：代理是转发，每个字节过网卡两次——客户端下载 1GB，数据先从上游进 VPS（`rx`≈1GB）、再转发给客户端出 VPS（`tx`≈1GB），所以网卡上 `rx+tx`≈**2GB**。在"只按出站计费"的机器上（**绝大多数**），用 `rx+tx` 会把真实用量按 **2 倍**算，导致 `LIMIT_GB` 在真实用量约**一半**时就误停机、客户端流量也显示成 2 倍。所以默认用 `tx` 最贴合真实计费；**只有确认商家是按 `rx+tx` 双向计费，才改成 `rx+tx`**（这种商家其实很少）。

如果是 Google Cloud 这类主要看 Internet 出站免费额度的机器，可以用：

```text
COUNT_MODE=tx
LIMIT_GB=180
```

这样按出站流量限速，并给 200GiB 免费额度留约 20GiB 缓冲。不要卡到 200GiB 整数，因为云厂商账单和 `vnstat` 的统计口径、延迟、GiB/GB 显示可能不完全一致。

网卡名手动核对：

```bash
ip -br link
```


## 部署前须知

### 甲骨文机器注意事项（Oracle Cloud）

甲骨文机器单独按“轻量、低风险、少常驻服务”处理，尤其是免费 ARM / 小内存实例：

```text
不要装：哪吒探针、宝塔、1Panel、3x-ui / s-ui 这类公网管理面板。
不要跑：HY2 / TUIC 这类 UDP/QUIC 直连协议做主力，也不要做 UDP 压测。
不要堆：Komari、cloudflared、网页管理面板、BBRv3 Max、各种探针一起常驻。
BBR3：普通 BBR 先开够用；CloudPassenger/Cloud-Kernel-BBRv3 有 arm64 包，可以试，但不是甲骨文主力机的必需项。
```

推荐组合：

```text
AnyTLS + VLESS Reality + SS2022(TCP 优先)
需要保底时再加 CF-Vless-WS
订阅仍用随机路径，管理仍走 SSH
```

原因很简单：甲骨文小机子更适合安静自用，不适合高频探针、重面板、多人共享、长时间满速下载或大 UDP 流量。面板类工具会额外占内存、开公网管理入口，还容易把防火墙、Nginx、证书、端口改乱；探针类工具会增加常驻进程和网络行为；HY2/TUIC 这类 UDP/QUIC 在部分网络和平台策略下更容易出现不可控的丢包、限速、风控或误判。

甲骨文机器的操作原则：

```text
只保留必要服务：sing-box + nginx + cron/vnstat。
只开放必要端口：22/tcp、80/tcp、443/tcp、4434/tcp、4435/tcp；不用 HY2 就不要开 4433/udp。
Oracle 控制台安全列表 / NSG 和系统防火墙是两层，都要同步检查。
日志保持 warn，别长期开 debug/info。
改配置前先 backup，出问题优先回滚，不要叠加安装更多面板排查。
换 BBR3 内核前必须确认控制台 / 救援模式可用，且不要删除官方原内核。
```

甲骨文免费机器的带宽认知：

```text
AMD 免费 Micro（VM.Standard.E2.1.Micro）：公网方向最高约 50 Mbps；同区域 / 私网方向最高约 480 Mbps。
ARM A1.Flex：按 OCPU 给网络上限，官方规格是 1 Gbps / OCPU，最高 40 Gbps。
所以 1 OCPU ARM 理论上限约 1 Gbps，2 OCPU 约 2 Gbps；它不是 AMD Micro 那个 50 Mbps 档。
实际测速会受地区、邻居、国际出口、晚高峰、协议、客户端网络影响，不保证长期跑满官方上限。
如果 ARM A1.Flex 实测长期只有 45-55 Mbps，先确认不是开错 AMD Micro；确认是 A1 后再考虑重启、改 shape、换可用区或换测试节点。
免费账户通常还有每月约 10 TB 出站免费流量额度；这是流量额度，不是实时带宽上限。
```

### 重装系统脚本：bin456789/reinstall（可选，谨慎）

项目地址：

```text
https://github.com/bin456789/reinstall
```

它的定位就是“一键 DD / 一键重装 VPS 系统”。可以从当前 Linux / Windows 直接重装到新的 Linux 或 Windows，也支持 ARM 服务器，适合在商家后台没有合适镜像、想跨发行版重装、或想把系统重新刷干净时使用。

但它不是普通安装软件，而是会接管启动流程并重写硬盘：

```text
会清除当前系统整个硬盘的全部数据，包括其它分区。
执行后如果还没重启，可以用 reset 取消；一旦进入安装/写盘阶段，就按“重装系统”处理。
不支持 OpenVZ / LXC；KVM / 云服务器 / 独服才适合。
必须先确认有控制台 / VNC / 串行控制台 / 救援模式，防止网络配置失败后 SSH 进不去。
```

甲骨文 ARM 机器的取舍：

```text
优先：Oracle 控制台直接 Reimage，选 Ubuntu 24.04 Minimal aarch64。
备选：控制台没有想要的镜像，或需要跨系统重装时，再考虑 bin456789/reinstall。
不要：在主力机、无备份、无控制台、无救援入口的情况下直接跑。
```

跑之前必须备份：

```bash
# 代理节点先备份凭证和配置
sudo bash install.sh backup 2>/dev/null || true

# 至少把这些文件另存到本地
/etc/sing-box/
/etc/sing-box-node.env
/etc/nginx/
/etc/cron.d/traffic_limit
```

Linux 下下载脚本：

```bash
curl -O https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh || wget -O reinstall.sh https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh
```

甲骨文 ARM 推荐目标仍然是 Ubuntu 24.04 Minimal：

```bash
bash reinstall.sh ubuntu 24.04 --minimal
```

更安全的做法是带上 SSH 公钥，避免重装后密码登录不通：

```bash
bash reinstall.sh ubuntu 24.04 --minimal --ssh-key "ssh-ed25519 AAAA... your-key"
```

如果只是想先验证安装环境和网络，不立刻开始写盘，可以用脚本的 hold 模式（具体参数以仓库 README 为准）：

```bash
bash reinstall.sh ubuntu 24.04 --minimal --hold 1
```

重装后要做的第一批检查：

```bash
uname -m          # 甲骨文 ARM 期望 aarch64
cat /etc/os-release
ip addr
ip route
systemctl status ssh --no-pager
sudo apt update && sudo apt upgrade -y
```

本文档结论：

```text
bin456789/reinstall 可以实现“脚本重装系统”，但风险级别等同 DD / 重装系统。
能用云厂商后台 Reimage 就优先后台 Reimage。
要用脚本，先备份、先确认控制台、优先带 SSH 公钥、目标系统选 Ubuntu 24.04 Minimal aarch64。
```

### 机器要求（最低）

```text
系统：推荐 Debian 11+ / Ubuntu 20.04+，必须有 systemd（Alpine/OpenRC 不支持）。
      本文命令按 Debian/Ubuntu 写（apt、cron、/etc/nginx/sites-enabled）。
      RHEL 系（Rocky/Alma/CentOS 8+）能跑但未逐条验证：需自行把 apt 换 dnf、
      装 cronie 代替 cron、nginx 配置路径改成 /etc/nginx/conf.d/。
架构：x86_64 或 arm64
配置：主线四协议 1 核 / 512MB 内存 / 5GB 硬盘可用；如果还跑 cloudflared、Komari、Codex、面板、BBRv3 Max，建议 1GB+ 内存
权限：root（或能 sudo）
网络：能访问外网装包（apt/dnf + sing-box 官方源）
域名：可选，不强制（见下）
```

低配 VPS 额外护栏：

```text
真实可用内存低于 512MB（例如面板显示 512MB、系统实际只有 410MiB）时，不要把所有可选件都当常驻服务。
主线只需要 sing-box + nginx + cron/vnstat；cloudflared、Komari、Codex、网页管理面板都属于额外开销。
如果要保留这些组件，至少做 1G swap，并把 sing-box / cloudflared 日志降到 warn。
排查“节点全挂”时先看 load / I/O wait / D 状态，不要只盯 sing-box 是否 active。
```

### 探针 Ping / TCPing 任务建议

探针里的 Ping 任务不要只看一个数字。海外 VPS 访问国内时，建议把 ICMP 和 TCPing 分开理解：

```text
ICMP Ping：看基础链路是否稳定、有没有丢包。
TCPing：更接近真实访问体验，尤其是国内网站 / API / CDN 的 TCP/TLS 连接。
```

海外 VPS 访问国内，优先看 TCPing；ICMP 只作为链路参考。原因是国内网站通常有 CDN / Anycast / 运营商调度，域名解析到的节点会变；而且真实网页/API 访问走 TCP/TLS，不是 ICMP。

推荐最简任务：

```text
国内-阿里DNS-ICMP      223.5.5.5

国内-广东移动-TCP      gd-cm-v4.ip.zstaticcdn.com:80
国内-广东电信-TCP      gd-ct-v4.ip.zstaticcdn.com:80
国内-广东联通-TCP      gd-cu-v4.ip.zstaticcdn.com:80
```

如果面板里是单独的 Ping 节点输入框，也可以填下面这组域名（dualstack 会按本机网络栈解析到 IPv4/IPv6，适合观察综合连通性）：

```text
中国电信测试节点      gd-ct-dualstack.ip.zstaticcdn.com
中国联通测试节点      gd-cu-dualstack.ip.zstaticcdn.com
中国移动测试节点      gd-cm-dualstack.ip.zstaticcdn.com
BD测试节点            lf3-ips.zstaticcdn.com
```

如果想看华东方向，再额外加上海三网：

```text
国内-上海移动-TCP      sh-cm-v4.ip.zstaticcdn.com:80
国内-上海电信-TCP      sh-ct-v4.ip.zstaticcdn.com:80
国内-上海联通-TCP      sh-cu-v4.ip.zstaticcdn.com:80
```

不建议把全国几十个 TCPing 任务都加进面板：页面会乱，低配机器和面板也会多一点无意义开销。自用节点一般保留「1 个 ICMP + 3 个 TCPing」就够；要更细才扩展到「1 个 ICMP + 6 个 TCPing」。

判断标准：

```text
ICMP 丢包 0%、抖动低：基础链路正常。
TCPing 稳定但延迟 200-300ms：海外 VPS 访问国内常见，不一定是故障。
TCPing 丢包 / 超时 / 波动很大：更接近真实访问卡顿，需要重点看线路或目标。
代理体验最终还是以客户端测速、下载、Google Play / 网页实际访问为准。
```

参考：

```text
NodeSeek 讨论：https://www.nodeseek.com/post-68572-1
Zstatic CDN 节点说明：https://lf3-ips.zstaticcdn.com/
```

按套餐总带宽做护栏，不按“几个人用”写死：

```text
商家写“公网带宽峰值 200 Mbps”时，通常表示入方向和出方向各自最高峰值 200 Mbps，
不是上下行合计 200 Mbps；但它是峰值，不是长期保证值。

代理节点最重要的是服务器出方向：你刷网页、视频、下载时，VPS 要把数据发回客户端。
HY2 的 down_mbps 按客户端视角理解，基本对应服务器出方向；up_mbps 对应服务器入方向。

稳定优先：不要把 HY2 直接顶到 200/200。
200 Mbps 峰值的小内存 VPS，建议先用 up_mbps=80、down_mbps=160。
人数变多时大家共享这条总量护栏，避免压力测试或多人同时下载把 UDP 队列、I/O wait 打爆。
如果连续观察 CPU / I/O wait / UDP Recv-Q 都很低，再逐步加到 180；不建议默认拉满 200。
```

> 2026-06-17/18 阿里云美国 200 Mbps 小机因 HY2/UDP 压测触发 DDoS 清洗的完整实测与处置记录（含当时的 UDP 开启状态表），已移至文末「实例记录」。通用结论就是上面的带宽护栏规则：小内存 / 小盘 / 峰值带宽机器不做压测，HY2 保留服务端限速。


### 要开哪些端口（云安全组 + 本机 ufw 两层都要放行）

云安全组（服务商控制台：阿里云 / 腾讯云 / Oracle / AWS / Vultr 等）是**最容易漏的一层**，它默认拒绝入站、而且经常单独拦 UDP。下面这些是“按本手册默认端口”需要放行的：

| 端口 | 协议 | 用途 | 是否必开 |
|---|---|---|---|
| `22` | tcp | SSH | 必开（改过 SSH 端口就放你那个） |
| `80` | tcp | Nginx 订阅 | 必开 |
| `443` | tcp | VLESS Reality | 必开 |
| `4433` | **udp** | Hysteria2 | 必开（默认四协议）；清洗敏感机变体见表下注 |
| `4434` | tcp | AnyTLS | 必开 |
| `4435` | tcp+udp | Shadowsocks-2022 | 必开（默认 TCP+UDP，与订阅模板 `udp: true` 对应）；清洗敏感机变体见表下注 |
| 端口段（如 `20000-21000`） | udp | HY2 端口跳跃 | 仅启用端口跳跃才需要，要放**整段 UDP** |

> 清洗敏感机变体：已经因为 HY2/UDP 触发过 DDoS 清洗的机器（完整经过见文末「实例记录」），把 `4433/udp`（必要时连同 `4435/udp`）改成不开，并同步从 sing-box、订阅、看板页和云安全组里移除对应协议。不要只从客户端删节点——服务端还监听就等于风险还在。

> **端口段语法别照抄错**：上表的 `20000-21000`（连字符）是给**云安全组控制台**和 nftables 用的写法；**ufw 要用冒号** `ufw allow 20000:21000/udp`（ufw 不认连字符范围，照抄会报错）。

> **不要对外开**的端口：CF-Vless 的本地入站 `127.0.0.1:28080`、网页管理面板 `127.0.0.1:8088`——它们只监听本机，靠 SSH 隧道访问，**绝不要**在安全组 / ufw 放行。
>
> 自检口诀：本机 `ss -lntup` 只能证明“本地在监听”，证明不了“外部能连进来”。**“HY2 / SS 连不上、但 Vless 正常”99% 是云安全组没放对应 UDP。**

### 域名要不要

```text
不强制：裸 IP 也能用（订阅 URL 和节点 server 都可以直接写 IP）。
有域名的好处：订阅地址好记、能走 Cloudflare 加密订阅、纯 IPv6 机器更好用。
本手册示例多用域名 SUB_DOMAIN_1；纯裸 IP 部署时：
  - 订阅 server 的 server_name 直接填这台机的 IP；
  - DNS 那一步跳过；
  - 订阅 URL 用 http://<本机IP>SUB_PATH（SUB_PATH 同样整篇替换成真实路径）。
```

域名角色建议固定分工，别混用：

| 变量 | 用途 | 建议 |
|---|---|---|
| `SUB_DOMAIN_1` | 订阅入口，回源到 Nginx 80 | 例如 `sub.example.com` |
| `CF_HOSTNAME` | CF-Vless 大保底节点入口，回源到 `127.0.0.1:28080` | 例如 `cf-us.example.com` |
| `CF_SUB_HOSTNAME` | 可选：Cloudflare HTTPS 订阅专用入口，回源到 Nginx 80 | 例如 `sub-cf.example.com` |

> 技术上可以让一个 Cloudflare 域名按 path 同时承载订阅和 WS 节点，但不建议作为默认做法。订阅入口是“管理/分发”，CF-Vless 是“代理节点”，混在同一个域名上会让回源、缓存、日志、故障排查都变复杂。大保底挂了不应该影响订阅能拉；订阅域名被扫也不应该牵连 WS 节点。

### 关于流量限制（不是每台机器都要限）

机器分两种，**按你这台的实际情况选**，不要照抄一个固定额度：

```text
有限流量（多数 VPS，按月给额度）：
   按「运行参数」把 LIMIT_GB 设成你的真实额度（建议留点缓冲，见该节换算说明），
   超额会自动停 sing-box，避免产生超额费 / 被停机。

无限流量（不限量套餐 / 大流量小鸡）：
   你不想被停机，两种做法二选一——
   ① 把 LIMIT_GB 设得很大（例如 999999），限流脚本就只「显示用量」、永不停机；（推荐）
   ② 完全不要流量显示：不仅跳过 traffic_limit.py，还要删掉 /etc/nginx/snippets/sub_headers.conf，
      并把所有 include 了它的 server 块里那行删掉，再 reload nginx。用
      `grep -rn sub_headers.conf /etc/nginx/` 找全所有 include（做过 443 SNI 分流的机器不止一处），
      漏删任何一处会因 include 目标不存在直接 `nginx -t` 失败、订阅整体不可用。
      若只跳过 traffic_limit.py 而保留 Nginx 的 include，客户端会一直显示 download=0
      （占位头不会被刷新），并不是「不显示流量」。
```

> 即便是无限流量，**留着限流脚本 + 设个大额度**仍有用：客户端能看到本月用了多少，方便发现异常（比如被人盗用订阅猛跑流量）。所以推荐做法①。


### GCP Standard Tier + EdgeOne 流量费用注意事项（GCP 免费机重点）

适用场景：GCP 免费/低成本节点、GCP Standard Tier 200GB 免费出站额度、EdgeOne/Cloudflare/CDN 前置、以及担心 CDN Interconnect/Carrier Peering 出账的机器。

先记住一条：**EdgeOne 的免费流量和 GCP 的源站出站流量不是同一个账本**。

```text
用户 -> EdgeOne -> GCP 源站
```

这条链路里：

```text
用户 -> EdgeOne
  算 EdgeOne 的加速侧流量。按 2026-07-03 腾讯官方文档，EdgeOne 免费版“安全加速流量/请求数”为不限量，
  但免费版没有 SLA，且滥用、攻击、异常大流量可能被平台限速、调度或封禁。

EdgeOne -> GCP 源站
  算 GCP 源站出站/回源相关流量，不会因为 EdgeOne 免费就自动免掉。
  缓存命中越高，GCP 回源越少。
```

GCP Standard Tier 的重点：

```text
1. Standard Tier 每个区域每月有 200GB 免费用量，按 billing account 下所有项目合并计算。
2. 默认通常是 Premium Tier；要用 Standard Tier，必须给 VM / LB 使用 Standard Tier 的区域外部 IP。
3. Cloud CDN 永远是 Premium Tier，不能拿来吃 Standard Tier 的 200GB 免费额度。
4. Standard Tier 是省钱路线，不是性能最优路线；它更多走公网/区域出口，延迟和稳定性可能不如 Premium。
```

检查一台 GCP VM 的外部 IP 是否为 Standard Tier：

```bash
# 替换 INSTANCE_NAME / ZONE
gcloud compute instances describe INSTANCE_NAME \
  --zone ZONE \
  --format='table(name,zone.basename(),networkInterfaces[0].accessConfigs[0].natIP,networkInterfaces[0].accessConfigs[0].networkTier)'
```

创建 Standard Tier 区域静态 IPv4 的示例：

```bash
# 替换 REGION，例如 us-west1 / us-central1 / us-east1
gcloud compute addresses create NODE_STD_IP \
  --region REGION \
  --network-tier=STANDARD
```

如果已有 VM 用的是 Premium 外部 IP，要切 Standard 通常需要换外部 IP；换 IP 会影响 DNS、订阅、客户端配置，操作前先备份并规划停机窗口。

#### 关于屏蔽 Akamai / Cloudflare / Fastly

Google 的 CDN Interconnect 支持的典型服务商包括：

```text
Akamai
Cloudflare
Fastly
```

当 GCP 节点向这些 CDN 网络大量出站时，账单里可能不是普通 Standard Tier Internet Egress，而是类似：

```text
Network Data Transfer Out via Carrier Peering Network
CDN Interconnect / Carrier Peering
```

这类流量可能不吃你预期的 Standard Tier 200GB 免费池，所以：

```text
如果这台 GCP 是“代理节点 / 下载节点 / 转发节点”：
  屏蔽 Akamai / Cloudflare / Fastly 的部分 IP/ASN，是一种账单保护手段，
  可以避免用户流量大量打到这些 CDN，触发 CDN Interconnect/Carrier Peering 计费。

如果你的服务本身套了 Cloudflare / EdgeOne / Fastly / Akamai：
  不要乱屏蔽对应 CDN，否则 CDN 无法回源，网站会 521/522/回源失败。
```

也就是说，**屏蔽 CDN 是为了保护“代理出站账单”，不是为了让 Cloudflare/EdgeOne 回源更省钱**。

推荐判断流程：

```text
1. 只做订阅页/看板页：
   EdgeOne/Cloudflare 橙云只代理 Nginx 订阅网页即可，节点本身仍写 GCP Standard IP 或灰云域名。

2. 做四协议代理节点：
   Hysteria2 / Reality / AnyTLS / SS2022 的 server 不要写 EdgeOne/Cloudflare 橙云域名；
   它们应直连 GCP Standard IP / 灰云域名。

3. 担心 GCP Standard 免费额度被 CDN Interconnect 吃掉：
   先看 GCP Billing 里是否出现 Carrier Peering / CDN Interconnect 相关 SKU；
   确认有问题后，再按需屏蔽 Akamai / Cloudflare / Fastly。

4. 如果屏蔽后大量网站打不开：
   这是正常副作用，因为很多网站本来就在 Cloudflare/Akamai/Fastly 后面。
   这类屏蔽适合“账单安全优先”的免费机，不适合日常全能代理。
```

#### EdgeOne 在本文档里的推荐角色

EdgeOne 和 Cloudflare 橙云一样，默认只建议承担“订阅/看板前置”角色：

```text
订阅/看板：
  客户端 -> EdgeOne/Cloudflare -> VPS Nginx:80 -> 返回订阅 YAML/base64/看板

主线四协议节点：
  客户端 -> GCP Standard IP / 灰云直连域名 -> VPS sing-box
```

不要把 EdgeOne 域名直接写进 Hysteria2 / Reality / AnyTLS / SS2022 的 `server:`，除非你明确做的是 WebSocket/Tunnel/HTTP 可代理形态。

实用结论：

```text
EdgeOne 免费版：适合吃前端访问流量，尤其是订阅页、静态页、看板页。
GCP Standard Tier：适合吃源站/节点直连出站，每区域每月 200GB 免费，但要确认 IP 是 Standard。
屏蔽 Akamai/Cloudflare/Fastly：是规避 GCP CDN Interconnect/Carrier Peering 出账的一种账单保护手段。
缓存命中：EdgeOne 缓存命中越高，GCP 回源越少，越省源站流量。
```
### 部署顺序与“完成度”（重要，别把可选件当成已部署）

这份文档分**主线**和**可选增强**两层，别混为一谈：

```text
主线（本文档纯手工就能搭完，搭完即可用）：
   四协议节点(HY2 / AnyTLS / Vless / SS2022) + Mihomo 订阅 + 国内直连分流

可选增强（没做完之前，不算“已部署”；确认跑通后再纳入常用配置）：
   CF-Vless 大保底（= Argo 隧道）、WARP 解锁、看板页、网页管理面板、BBRv3 换内核 等
```

推荐执行顺序（从稳到险，一步跑通再上下一步）：

```text
1. 先把四协议主线跑通（sing-box check 过、订阅能拉、客户端能连）
2. 再加流量显示/限流 + 订阅传输保护（随机路径，必要时 CF 加密订阅）
3. 网络加速第一步 sysctl（16MB UDP 缓冲，对 HY2 收益最大，免重启）
4. 最后再做高风险增强：BBRv3 按机型选档（低配/清洗敏感/生产机走标准版，仅测速机走 Max + 菜单 12，见「网络加速」一节）；CF 大保底 / WARP 按需加
```

> 特别提醒 **CF-Vless 大保底（Argo 隧道）**：整条链路（cloudflared、本地入站、Tunnel 绑定、公网 `101` 验证）全部通过后才算部署完成、才能加进订阅，否则就是**死节点**——完整判定标准和验证命令见「CF-Vless-WS 保底部署」一节。

## 最终架构

推荐端口（HY2_PORT / ANYTLS_PORT / VLESS_PORT 的默认值）：

```text
22/tcp       SSH
80/tcp       Nginx 订阅
443/tcp      VLESS Reality Vision
4433/udp     Hysteria2
4434/tcp     AnyTLS
4435/tcp+udp Shadowsocks-2022
```

推荐文件：

```text
/etc/sing-box/config.json
/etc/sing-box/server.crt
/etc/sing-box/server.key
/etc/sing-box-node.env
/var/www/htmlSUB_PATH
/etc/nginx/sites-enabled/default
/etc/nginx/snippets/sub_headers.conf
/usr/local/bin/traffic_limit.py
/etc/cron.d/traffic_limit
/var/lib/sing-box-node/        # traffic_limit.py 自建状态目录，无需手动创建；存放 quota-stopped
```

推荐权限：

```text
/etc/sing-box/config.json             600 root root
/etc/sing-box/server.key              600 root root
/etc/sing-box-node.env                600 root root
/usr/local/bin/traffic_limit.py       700 root root
/var/www/htmlSUB_PATH              644 root root
/etc/nginx/snippets/sub_headers.conf  644 root root
```

## 四个协议

客户端节点名只保留协议名：

```text
Hysteria2
AnyTLS
Vless
SS2022
```

实际协议组合：

```text
Hysteria2 = UDP/QUIC + TLS + h3 + SNI www.bing.com
AnyTLS    = TCP + TLS + SNI www.bing.com
Vless     = VLESS + TCP + Vision + uTLS chrome + REALITY + www.bing.com
SS2022    = Shadowsocks-2022（2022-blake3-aes-128-gcm，AEAD），TCP + UDP
```

使用建议：

```text
1. 优先试 Hysteria2，适合 UDP 正常的网络
2. HY2 不稳时切 AnyTLS，TCP 兼容性更好
3. 排查或兜底时切 Vless，Reality + Vision 稳定性好
4. 前面都不行再试 SS2022，协议简单、客户端兼容性最好，最不容易出兼容性问题
```

`Vless` 只是显示名。实际仍然是 `VLESS Reality Vision`，不是把 Reality 或 Vision 去掉了。

### CF WS 节点命名规范

`CF-Vless` 是技术类别名称；客户端实际显示名称统一使用 AWS 风格：

```text
CF-<SERVER>-WS
CF-<SERVER>-WS-01
CF-<SERVER>-WS-02
```

`<SERVER>` 使用稳定的大写机器代号，不使用 IP、完整域名或账号信息。当前示例：

```text
CF-GCP-WS
CF-AWS-WS
CF-ALIYUN-WS
CF-CLOUDNIUM-WS
CF-PKQ-WS-01
CF-PKQ-WS-02
```

同一条 sing-box 入站如果通过两个不同公网域名提供备用入口，主入口使用 `CF-<SERVER>-WS-01`，同一入站的第二个域名使用 `CF-<SERVER>-WS-01-ALT`。这不是第三条节点，不能把它误编号为新的独立入站。

改名时必须同步更新 Clash `name`、通用订阅 URI 的 `#` 显示名、看板展示名及 `select` 代理组引用。服务端 tag 可使用小写对应格式，例如 `cf-vless-aws-ws-in`；改 tag 前先检查路由、环境文件和脚本引用。显示名或 tag 重命名绝不能同时改 UUID、WS 路径、域名、端口或 TLS 参数。

### 按机型选协议组合（先看这张表，再决定开哪些 UDP 端口）

文档默认按「四协议全开」写。但甲骨文、阿里云清洗敏感机等不适合跑满 UDP，照抄默认反而会踩坑。先按机型确定组合，其余章节（sing-box 入站、订阅模板、防火墙）里的 UDP 相关项都按这张表取舍：

| 机型 | 推荐组合 | HY2 4433/udp | SS2022 4435/udp |
|---|---|---|---|
| **普通 KVM / 大多数 VPS** | 四协议全开（默认） | 开 | 开（订阅 `udp: true`） |
| **甲骨文 ARM / 小内存机** | AnyTLS + Vless + SS2022(TCP) + 可选 CF-Vless | 不开 | 不开（订阅 `udp: false`） |
| **阿里云等清洗敏感机** | 稳定优先去 HY2；速度优先可留但**必须限速** | 视清洗情况：去掉，或限速 `up 80/down 160` | 按需，触发清洗就关 |
| **纯 IPv6 机** | 同机型再叠加，CF-Vless 当重要保底 | 按上 | 按上 |

> 关掉某条 UDP 时，服务端入站、订阅 `udp` 字段、云安全组、ufw **四处一起改**，不要只删客户端节点——服务端还监听就等于风险还在（完整清单见「要开哪些端口」表下注）。甲骨文/清洗敏感机的取舍原因见「部署前须知」和文末「实例记录」。
>
> 走一键脚本时不用手改这四处：`ENABLE_HY2=0 bash install.sh` 去掉 HY2（服务端入站、订阅、分享链接、ufw、端口跳跃一并处理），`SS_UDP=0` 让 SS2022 走 TCP-only（服务端加 `network: tcp`、订阅写 `udp: false`、不放行 SS 的 UDP）。两个值都会存进 `/etc/sing-box-node.env`，重装和 `cf`/`warp` 不会把协议加回来。

> 版本下限：AnyTLS 需要 **sing-box ≥ 1.12.0** 和 **mihomo ≥ 1.19.3**；SS2022 几乎所有现代客户端都支持。装完后用 `sing-box version` 确认，旧版没有 anytls 入站会直接 `sing-box check` 报错（这时可去掉 anytls 入站，保留 HY2 + Vless + SS2022 三条）。
>
> SS2022 的密码是 **base64**（`openssl rand -base64 16`），分享/填写时不要当成 hex；密码字节数要和加密方法匹配（128-gcm=16 字节，256-gcm / chacha20=32 字节），不匹配 `sing-box check` 会报错。
>
> SS2022 还会校验时间戳。客户端本机时间如果慢/快几十秒，服务端日志会出现 `bad timestamp`，客户端通常只显示 Timeout。Windows 客户端异常时先用 `w32tm /stripchart /computer:time.cloudflare.com /samples:5 /dataonly` 看偏移，必要时管理员 PowerShell 执行 `w32tm /resync /force`。

## 大保底协议

如果担心 VPS 直连 IP 被墙，推荐额外准备一条 Cloudflare 保底通道：

```text
CF-Vless = VLESS + WebSocket + Cloudflare Tunnel
```

> **CF-Vless 节点 和「Argo 隧道」节点是同一个东西，不是两个节点。**
> 「Argo Tunnel」是 Cloudflare 这条隧道的**旧名/别名**，现在官方叫「Cloudflare Tunnel」，底层都是 `cloudflared` 这个守护进程。本节用 `cloudflared` 建的隧道**就是** Argo 隧道，所以这一条 CF-Vless 节点 = 走 Argo/Cloudflare 隧道的 VLESS-WS 节点。**一个节点、两个叫法，不要再单独加一个「Argo 节点」，加了就是重复。**
>
> 另外，Cloudflare 隧道有两种，别混：
> ```text
> 命名隧道（Named Tunnel）：绑你自己的域名 CF_HOSTNAME，地址固定不变。
>                          本文档用的就是它，适合做订阅里的稳定保底节点。
> 临时隧道（Quick Tunnel / trycloudflare）：cloudflared tunnel --url ...，
>                          Cloudflare 随机给个 xxx.trycloudflare.com，免域名免账号，
>                          但每次重启域名都变、速度更差、随时可能失效。
>                          只适合临时测试，绝不能放进订阅当固定节点（URL 一变就成死节点）。
> ```
> 有些教程说的「Argo 节点」特指这个临时隧道——做保底不能用它，所以本文档坚持用**命名隧道**。

定位：

```text
平时不用
直连 IP 出问题时临时切换
速度不追求最高，优先保证能连
```

为什么选它：

```text
客户端连接 Cloudflare 域名，不直接连接 VPS IP
VPS 通过 cloudflared 主动连接 Cloudflare
即使 VPS IP 直连不可达，也有机会通过 Cloudflare 域名连接
和当前 VLESS / sing-box 体系一致，后续维护更顺
```

域名边界：

```text
CF_HOSTNAME 是大保底节点域名，只给 CF-Vless-WS 用。
SUB_DOMAIN_1 是订阅域名，只给 Clash/Mihomo 订阅和看板用。
不要为了省一个 DNS 记录，把订阅域名直接回源到 127.0.0.1:28080；那是 VLESS WS 入站，不是 Nginx。
如果要 Cloudflare HTTPS 订阅，优先新增 CF_SUB_HOSTNAME，让它回源到本机 80。
```

不建议把 Cloudflare 保底当主力：

```text
Cloudflare 链路更长
速度不一定比直连好
配置依赖 Cloudflare Tunnel 和域名路由
排查点比直连多
Tunnel down 时 CF-Vless 会单独挂，但不代表直连四协议都坏了
```

特殊 VPS 注意：

```text
如果 VPS 商家会对 Cloudflare / Tunnel / CDN 相关流量额外计费，或者明确不适合走 Cloudflare，
这一台就不要启用 CF-Vless。
做法是：订阅里不写 CF-Vless，sing-box 不保留 cf-vless-ws-in，cloudflared 停止并禁用。
其它普通 VPS 仍然可以按需使用 CF-Vless 作为大保底。
```

推荐最终节点排序：

```text
Hysteria2
AnyTLS
Vless
SS2022
CF-Vless
```

死节点问题同前（见「部署顺序与“完成度”」的特别提醒）：绑定并验证通过前，不要把 `CF-Vless` 写进客户端订阅。

如果客户端当前手动选中了 `CF-Vless`，而 Cloudflare Tunnel 挂了，客户端会表现为“所有流量都断”。这时先手动切回 `Hysteria2` / `AnyTLS` / `Vless` / `SS2022` 验证直连节点，不要把 `CF-Vless` 的故障误判成整台 VPS 全部节点死亡。

## 部署基础环境

SSH 登录 VPS：

```bash
ssh root@SERVER_IP
# 纯 IPv6 机器没有 IPv4 SERVER_IP，改用 IPv6 地址或域名：
#   ssh root@SERVER_IPV6        # 例 ssh root@2001:db8::1
#   ssh root@SUB_DOMAIN_1       # 已配好 AAAA 时更省事
```

安装依赖：

```bash
apt update
# python3 给限流脚本用；ca-certificates 保证后面 curl https 源不报证书错。极简镜像可能两者都没有。
apt install -y curl wget unzip tar jq nginx vnstat openssl cron python3 ca-certificates
systemctl enable --now nginx
systemctl enable --now vnstat
systemctl enable --now cron

# 时间同步：Reality / TLS 握手对时钟敏感（偏差过大握手会被拒，且很难定位）
if ! timedatectl set-ntp true 2>/dev/null; then
  # 极简镜像可能没装 systemd-timesyncd；若机器已用 chrony 且 timedatectl 成功，就不会走到这里。
  apt install -y systemd-timesyncd
  systemctl enable --now systemd-timesyncd
  timedatectl set-ntp true
fi
timedatectl
```

配置防火墙（如果主机启用了 ufw）：

```bash
# 没装 ufw 时，整段自动跳过（只靠云安全组放行端口），不会报 "ufw: command not found"。
# 要用 ufw 再先 apt install -y ufw。
if command -v ufw >/dev/null; then
  # 很多镜像默认装了 ufw 但未启用；如果你要启用，先放行这些端口再 enable，否则会把自己 SSH 关在外面
  ufw allow 22/tcp
  ufw allow 80/tcp
  ufw allow 443/tcp
  ufw allow 4434/tcp
  ufw allow 4435/tcp     # Shadowsocks-2022 TCP
  # 默认四协议主线，下面两条 UDP 也必须放行（漏掉就是"HY2/SS 连不上、Vless 正常"）：
  ufw allow 4433/udp     # Hysteria2
  ufw allow 4435/udp     # SS2022 UDP
  # 清洗敏感机变体（见「要开哪些端口」表下注）：注释掉上面两条 UDP，
  # 并同步从 sing-box / 订阅 / 云安全组移除对应协议。
  # 如果启用了 HY2 端口跳跃，还要放整段 UDP，例如：
  # ufw allow 20000:21000/udp
  # ufw enable    # 确认上面都放行后再执行
  ufw status
else
  echo "本机未安装 ufw，跳过这一段（端口放行交给云安全组）"
fi
```

> 云安全组是另一层，必须在服务商控制台（阿里云/腾讯云/Oracle/AWS 等）同样放行以上端口，**尤其 `4433/udp`**。云安全组默认拒绝入站、而且经常拦 UDP，这是“HY2 连不上、Vless 却正常”的头号原因。注意：本机 `ss` 检查只能证明本地在监听，证明不了外部能连进来。

安装 sing-box：

```bash
curl -fsSL https://sing-box.app/install.sh | sh
sing-box version    # 确认 >= 1.12.0（anytls 入站需要）
```

生成参数（每条都标注了对应变量）：

```bash
sing-box generate uuid               # -> VLESS_UUID
sing-box generate reality-keypair    # PrivateKey -> REALITY_PRIVATE_KEY, PublicKey -> REALITY_PUBLIC_KEY
openssl rand -hex 16                 # -> HY2_PASSWORD
openssl rand -hex 16                 # -> ANYTLS_PASSWORD（再跑一次，要和上面不同）
openssl rand -hex 8                  # -> REALITY_SHORT_ID
openssl rand -base64 16              # -> SS_PASSWORD（base64！128-gcm 用 16 字节；若用 256-gcm/chacha 改成 -base64 32）
```

生成 HY2 / AnyTLS 自签证书（CN 用 TLS_SNI）：

```bash
mkdir -p /etc/sing-box
openssl req -x509 -nodes -newkey rsa:2048 \
  -keyout /etc/sing-box/server.key \
  -out /etc/sing-box/server.crt \
  -days 3650 \
  -subj "/CN=www.bing.com"

chmod 600 /etc/sing-box/server.key
chmod 644 /etc/sing-box/server.crt
```

> HY2 / AnyTLS 客户端模板里是 `skip-cert-verify: true`，所以自签证书 CN 不会被严格校验；这里把 CN 写成 `TLS_SNI` 只是为了配置一致、方便排查。真正必须两端完全一致的是 Reality 的 `REALITY_SNI`。

## sing-box 服务端模板

创建：

```bash
nano /etc/sing-box/config.json
```

模板（端口/SNI 是推荐默认值；密码、UUID、Reality 密钥替换成你生成的）：

```json
{
  "log": {
    "disabled": false,
    "level": "warn"
  },
  "inbounds": [
    {
      "type": "hysteria2",
      "tag": "hy2-in",
      "listen": "::",
      "listen_port": 4433,
      "users": [
        {
          "password": "HY2_PASSWORD"
        }
      ],
      "tls": {
        "enabled": true,
        "certificate_path": "/etc/sing-box/server.crt",
        "key_path": "/etc/sing-box/server.key"
      }
    },
    {
      "type": "anytls",
      "tag": "anytls-in",
      "listen": "::",
      "listen_port": 4434,
      "users": [
        {
          "password": "ANYTLS_PASSWORD"
        }
      ],
      "tls": {
        "enabled": true,
        "certificate_path": "/etc/sing-box/server.crt",
        "key_path": "/etc/sing-box/server.key"
      }
    },
    {
      "type": "vless",
      "tag": "vless-in",
      "listen": "::",
      "listen_port": 443,
      "users": [
        {
          "uuid": "VLESS_UUID",
          "flow": "xtls-rprx-vision"
        }
      ],
      "tls": {
        "enabled": true,
        "server_name": "www.bing.com",
        "reality": {
          "enabled": true,
          "handshake": {
            "server": "www.bing.com",
            "server_port": 443
          },
          "private_key": "REALITY_PRIVATE_KEY",
          "short_id": ["REALITY_SHORT_ID"]
        }
      }
    },
    {
      "type": "shadowsocks",
      "tag": "ss-in",
      "listen": "::",
      "listen_port": 4435,
      "method": "2022-blake3-aes-128-gcm",
      "password": "SS_PASSWORD"
    }
  ],
  "outbounds": [
    {
      "type": "direct",
      "tag": "direct"
    }
  ]
}
```

已部署机器要给 HY2 加总带宽护栏时，不要手改一大段 JSON，直接用 `jq` 改 `hy2-in` 这一项。以 200 Mbps 峰值 VPS 为例：

```bash
ts=$(date +%Y%m%d-%H%M%S)
mkdir -p /root/vps-config-backups/$ts
cp -a /etc/sing-box/config.json /root/vps-config-backups/$ts/config.json

tmp=$(mktemp)
jq '(.inbounds[]? | select(.tag=="hy2-in")) |= (.up_mbps=80 | .down_mbps=160)' \
  /etc/sing-box/config.json > "$tmp"
sing-box check -c "$tmp"
install -m 600 "$tmp" /etc/sing-box/config.json
rm -f "$tmp"
systemctl restart sing-box
```

> 不要为了“禁 IPv6 出站”在 direct outbound 里加旧版 `domain_strategy: ipv4_only`。sing-box 1.13 已经会把这类 legacy 写法判为弃用，严重时会让 `sing-box check` 失败。无 IPv6 机器的出站策略后续要按新版 DNS / route 规则处理，不要把旧字段塞进 direct。

> 日志级别默认用 `warn`。`info` 会把大量连接记录写进 journald，低内存 / 低 IOPS VPS 在测速、Steam 下载、AI 工具长连接多的时候容易出现 I/O wait 飙高、SSH 卡顿、cloudflared 超时。需要排查具体连接时可以临时改 `info`，看完再改回 `warn` 并 `systemctl restart sing-box`。

> VLESS Reality 兼容性提示：如果客户端持续 `REALITY authentication failed`、而 SNI / short_id / 时间都已排除，可能是个别 sing-box + Mihomo 版本组合实测不通（见文末「实例记录·阿里云小机」）。这时可改用 Xray 专门承载 443/Reality、sing-box 只跑其余协议——这是个别机器的兜底路线，主线不需要装 Xray。

> 无 IPv6 的机器注意：上面四个入站（HY2 / AnyTLS / Vless / SS2022）都写的是 `"listen": "::"`（同时监听 IPv4/IPv6）。
> 大多数内核即使没有公网 IPv6 也能正常 bind `::` 并接受 IPv4 连接。但如果某台 VPS 的内核**禁用**了 IPv6（`net.ipv6.conf.all.disable_ipv6=1`），`::` 会 bind 失败导致 sing-box 起不来。
> 这种机器把四个入站的 `"listen": "::"` 全改成 `"listen": "0.0.0.0"` 即可。部署后无论哪种情况，都用下面的 `ss` 确认 IPv4 确实在监听：
>
> ```bash
> ss -lntup | grep -E ':(443|4433|4434|4435)\b'   # 要能看到 0.0.0.0:* 或 *:* 在监听
> ```

设置权限并检查：

```bash
chown root:root /etc/sing-box/config.json
chmod 600 /etc/sing-box/config.json
sing-box check -c /etc/sing-box/config.json
systemctl enable --now sing-box
systemctl restart sing-box
```

## Clash/Mihomo 订阅模板

> **客户端兼容性（重要）**：这份订阅面向 **Mihomo（Clash Meta）内核**，**不是**老的 Clash Premium / Clash Core。`anytls` 节点、`reality-opts`、`client-fingerprint`、SS2022 的 `2022-blake3-*` 等字段老 Clash 不认，硬导会报错或节点不可用。请用 Mihomo 系客户端：
>
> ```text
> 桌面：Clash Verge Rev / Mihomo Party / FlClash
> 手机：Clash Meta for Android（ClashMetaForAndroid）/ Hiddify / Stash(iOS) 等支持 Meta 的
> ```
>
> AnyTLS 还要求 **mihomo ≥ 1.19.3**（见「四个协议」一节）。只想要最稳兼容、不挑客户端，就主要用 Hysteria2 / Vless / SS2022，AnyTLS 当可选。

订阅文件路径建议使用随机路径：

```text
/var/www/htmlSUB_PATH
```

不要长期使用：

```text
/var/www/html/sub.yaml
```

创建：

```bash
nano /var/www/htmlSUB_PATH
```

模板：

```yaml
mixed-port: 7897
# 默认 false：不把代理端口暴露给局域网其它设备。
# 只有确实要给同网段的电视/其它手机共享这台客户端的代理时，才改成 true。
allow-lan: false
mode: rule
log-level: info
# IPv4-only 保持 false。纯 IPv6、或想让双栈节点走 IPv6 时，必须改成 true，
# 否则 mihomo 会禁用 AAAA 解析和 IPv6 连接（关闭 ipv6 时 AAAA 返回空解析），
# 纯 IPv6 机器照抄会连不上。详见后面「IPv4 / IPv6 / 双栈」。
ipv6: false
tcp-concurrent: true

# 节点连接地址统一用 SERVER_ADDRESS：
#   IPv4-only / 双栈默认：SERVER_ADDRESS 填 SERVER_IP（IPv4 字面量）。
#   IPv6-only：SERVER_ADDRESS 填域名 SUB_DOMAIN_1（别写 IPv6 字面量）。
# 注意：下面 rules 里的 IP-CIDR,SERVER_IP/32 仍用 IPv4 字面量，别和 SERVER_ADDRESS 混。
# 密码/UUID/公钥/short-id 一律加双引号：这些是随机 hex/base64 串，
# 万一随机出全数字（如 short-id、密码），YAML 会当成整数解析导致类型错误、节点静默失效。
proxies:
  - name: "Hysteria2"
    type: hysteria2
    server: SERVER_ADDRESS
    port: 4433
    password: "HY2_PASSWORD"
    sni: www.bing.com
    skip-cert-verify: true
    alpn:
      - h3

  - name: "AnyTLS"
    type: anytls
    server: SERVER_ADDRESS
    port: 4434
    password: "ANYTLS_PASSWORD"
    sni: www.bing.com
    skip-cert-verify: true
    udp: true
    client-fingerprint: chrome

  - name: "Vless"
    type: vless
    server: SERVER_ADDRESS
    port: 443
    uuid: "VLESS_UUID"
    network: tcp
    udp: true
    tls: true
    flow: xtls-rprx-vision
    servername: www.bing.com
    client-fingerprint: chrome
    reality-opts:
      public-key: "REALITY_PUBLIC_KEY"
      short-id: "REALITY_SHORT_ID"

  - name: "SS2022"
    type: ss
    server: SERVER_ADDRESS
    port: 4435
    cipher: 2022-blake3-aes-128-gcm
    password: "SS_PASSWORD"
    # 默认 true，与服务端 4435/tcp+udp、云安全组和 ufw 两层放行 4435/udp 对应；
    # 清洗敏感机走 TCP-only 变体时改成 false（并同步不放行 4435/udp）。
    udp: true

proxy-groups:
  - name: "🚀 节点选择"
    type: select
    proxies:
      - "Hysteria2"
      - "AnyTLS"
      - "Vless"
      - "SS2022"

rules:
  - DOMAIN,SUB_DOMAIN_1,DIRECT
  - DOMAIN,SUB_DOMAIN_2,DIRECT
  - IP-CIDR,SERVER_IP/32,DIRECT,no-resolve

  - DOMAIN-SUFFIX,local,DIRECT
  - DOMAIN-SUFFIX,localhost,DIRECT
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,100.64.0.0/10,DIRECT,no-resolve
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,169.254.0.0/16,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve
  # 198.18.0.0/16 是 fake-ip / RFC2544 基准测试保留网段，正常不应有真实业务。
  # 用 REJECT：客户端重启后应用缓存的旧 fake-ip 连接会立即失败并重新解析，
  # 而不是被 DIRECT 发往不可路由地址慢慢等超时。
  - IP-CIDR,198.18.0.0/16,REJECT,no-resolve
  - IP-CIDR,224.0.0.0/4,DIRECT,no-resolve

  # Google Play / GMS 下载链路放在国内直连规则前面。
  # 否则部分下载 CDN / 校验域名可能被 GEOIP,CN 或污染解析打到 DIRECT，
  # 表现为 Google Play 有下载进度但卡住、不到 100% 或安装失败。
  - GEOSITE,google,🚀 节点选择
  - DOMAIN-SUFFIX,google.com,🚀 节点选择
  - DOMAIN-SUFFIX,googleapis.com,🚀 节点选择
  - DOMAIN-SUFFIX,gstatic.com,🚀 节点选择
  - DOMAIN-SUFFIX,googleusercontent.com,🚀 节点选择
  - DOMAIN-SUFFIX,ggpht.com,🚀 节点选择
  - DOMAIN-SUFFIX,gvt1.com,🚀 节点选择
  - DOMAIN-SUFFIX,gvt2.com,🚀 节点选择
  - DOMAIN-SUFFIX,gvt3.com,🚀 节点选择
  - DOMAIN-SUFFIX,android.com,🚀 节点选择
  - DOMAIN-SUFFIX,google-analytics.com,🚀 节点选择

  - DOMAIN-SUFFIX,qq.com,DIRECT
  - DOMAIN-SUFFIX,weixin.com,DIRECT
  - DOMAIN-SUFFIX,wechat.com,DIRECT
  - DOMAIN-SUFFIX,gtimg.com,DIRECT
  - DOMAIN-SUFFIX,qpic.cn,DIRECT
  - DOMAIN-SUFFIX,bilibili.com,DIRECT
  - DOMAIN-SUFFIX,b23.tv,DIRECT
  - DOMAIN-SUFFIX,hdslb.com,DIRECT
  - DOMAIN-SUFFIX,taobao.com,DIRECT
  - DOMAIN-SUFFIX,tmall.com,DIRECT
  - DOMAIN-SUFFIX,jd.com,DIRECT
  - DOMAIN-SUFFIX,360buyimg.com,DIRECT
  - DOMAIN-SUFFIX,alicdn.com,DIRECT
  - DOMAIN-SUFFIX,aliyun.com,DIRECT
  - DOMAIN-SUFFIX,alipay.com,DIRECT
  - DOMAIN-SUFFIX,douyin.com,DIRECT
  - DOMAIN-SUFFIX,iesdouyin.com,DIRECT
  - DOMAIN-SUFFIX,byteimg.com,DIRECT
  - DOMAIN-SUFFIX,bytedance.com,DIRECT
  - DOMAIN-SUFFIX,amap.com,DIRECT
  - DOMAIN-SUFFIX,autonavi.com,DIRECT
  - DOMAIN-SUFFIX,baidu.com,DIRECT
  - DOMAIN-SUFFIX,bdstatic.com,DIRECT
  - DOMAIN-SUFFIX,163.com,DIRECT
  - DOMAIN-SUFFIX,126.net,DIRECT
  - DOMAIN-SUFFIX,127.net,DIRECT
  - DOMAIN-SUFFIX,mi.com,DIRECT
  - DOMAIN-SUFFIX,xiaomi.com,DIRECT
  - DOMAIN-SUFFIX,miui.com,DIRECT
  - DOMAIN-SUFFIX,huawei.com,DIRECT
  - DOMAIN-SUFFIX,vmall.com,DIRECT

  - GEOSITE,cn,DIRECT
  - GEOIP,CN,DIRECT
  # 屏蔽走代理流量的 QUIC（UDP 443）：逼浏览器回退 TCP+TLS 走代理，
  # 避免 QUIC-in-QUIC 双重拥塞控制和运营商 UDP QoS 造成的间歇性卡顿。
  # 必须放在国内直连规则之后、MATCH 之前：国内网站和局域网的 QUIC 不受影响。
  - AND,((NETWORK,UDP),(DST-PORT,443)),REJECT
  - MATCH,🚀 节点选择
```

> 这个基础模板故意不写 `dns:` 段，走客户端默认 DNS，降低手搓复杂度；`GEOSITE,cn` 和显式域名规则已经能覆盖大多数国内直连场景。需要 `fake-ip`、自定义 nameserver 或更细 DNS 分流时，再作为可选增强补，不影响主线可用性。

保存后设置属主和权限（前面「推荐权限」表里写了，这里实际执行）：

```bash
chown root:root /var/www/htmlSUB_PATH
chmod 644 /var/www/htmlSUB_PATH
```

如果没有第二个订阅域名，删除这一行：

```yaml
- DOMAIN,SUB_DOMAIN_2,DIRECT
```

为什么用 `select`：

```text
手机端手动切换最清楚
方便判断 HY2 / AnyTLS / Vless 哪条更稳
同一台 VPS 不需要复杂负载均衡
fallback 会自动切换，不利于排查
```

可选增强：自动容灾组（不改变上面"手动最清楚"的取舍——手动组仍是入口，只是多一个可选项）：

```yaml
proxy-groups:
  - name: "🚀 节点选择"
    type: select
    proxies:
      - "⚡ 自动容灾"     # 平时选它：当前协议挂了会在一个探测周期内自动切换
      - "Hysteria2"
      - "AnyTLS"
      - "Vless"
      - "SS2022"

  - name: "⚡ 自动容灾"
    type: url-test
    url: https://www.gstatic.com/generate_204
    interval: 300
    tolerance: 50
    proxies:
      - "Hysteria2"
      - "AnyTLS"
      - "Vless"
      - "SS2022"
```

排障时在客户端把 🚀 节点选择 切回具体协议即可，两头兼顾。注意：同一台 VPS 的四个协议共享同一条物理线路，url-test 只能容灾"某个协议被墙/被 QoS"，救不了整机断线。

> 客户端侧说明（Clash Verge Rev）：本机已通过订阅的"规则增强"prepend 了等价的 QUIC 屏蔽（带 `NOT GEOSITE,cn` 排除国内）和 198.18.0.0/16 REJECT。订阅模板更新部署后，客户端那两条 prepend 可删可留——语义重复但不冲突。

## 订阅传输安全

默认订阅走 `http://`（80 端口），订阅 YAML 里包含 UUID、密码、Reality 公钥等节点参数。明文 HTTP 下，能被动抓到这一次订阅请求的人就能拿到全套节点参数。随机路径是“藏”，不是“加密”。

一键脚本生成的可视化看板页同理：`/panel-xxxx.html` 虽然是随机路径，但页面里能看到 Clash 订阅、通用 base64 订阅、单节点分享链接和二维码，本质上也含全套节点凭证。**随机路径只能防扫描，不能当登录密码用**。如果看板 URL 被浏览器历史、聊天截图、日志、代理缓存或别人抓包拿到，对方就能直接打开看板并复制节点。

所以一键脚本部署后建议第一时间给看板页加登录：

```bash
cd /root/sing-box-oneclick 2>/dev/null || cd /root
sudo bash install.sh panel-pass "$(openssl rand -hex 12)"
sudo bash install.sh panel
```

如果脚本是在线跑的、本机没有 `install.sh`，先把脚本下载到当前目录再执行：

```bash
wget -O install.sh https://raw.githubusercontent.com/shiro1888/sing-box-oneclick/main/install.sh
sudo bash install.sh panel-pass "$(openssl rand -hex 12)"
sudo bash install.sh panel
```

看板登录的边界：

```text
它是暗色卡片风格的自定义登录页，不是浏览器自带 Basic Auth 弹窗。
它是 nginx 服务端校验，不是前端 JS 假密码；密码不写进看板源码。
但默认仍是 HTTP，cookie 明文传输；同网段/链路抓包仍可能拿到 cookie。
它主要挡“知道随机路径的人”，要真加密仍然要走 HTTPS / Cloudflare Tunnel。
panel-pass 的登录 map 不随 backup 迁移，换机 restore 后要重新设置一次。
```

自用、低频拉取、路径足够随机时，实际风险中等。按需选择加固：

```text
最低成本：保持随机 SUB_PATH / PANEL_PATH，不外传；看板页执行 panel-pass 加登录；必要时换路径（只挡住别人继续拉订阅）
更稳妥：按本节后面的「直连 HTTPS：Nginx 443 SNI 分流」让订阅域名直接走 HTTPS；
         或者已有 Cloudflare Tunnel 时，让订阅域名单独回源到 Nginx。
```

> 注意：换 `SUB_PATH` **不等于换凭证**。订阅 YAML 里的 `HY2_PASSWORD`、`ANYTLS_PASSWORD`、`SS_PASSWORD`、`VLESS_UUID`、Reality 公钥一旦被抓走，旧路径失效后这些值依然有效、节点照样能被别人连。
> 所以一旦怀疑某次订阅被截获，要做的是**重置节点凭证**（重新生成上述密码/UUID，更新 sing-box 服务端与订阅 YAML 两边，`systemctl restart sing-box`），换路径只是顺手一起做。

> 重要：不能直接用 `CF_HOSTNAME` 拉订阅。下面「CF-Vless-WS 保底部署」里把 `CF_HOSTNAME` 整个回源到了 `http://127.0.0.1:28080`，那是 VLESS WS 入站，**不是** Nginx，订阅请求打过去拿不到 YAML。要走 Cloudflare 加密订阅，必须让订阅命中本机的 Nginx（80 端口），二选一：

```text
方式 A（推荐，最不容易错）：另起一个订阅专用域名 CF_SUB_HOSTNAME，
        在 Tunnel 里单独把它回源到 http://127.0.0.1:80，
        订阅地址用 https://CF_SUB_HOSTNAMESUB_PATH

方式 B（共用一个域名，按 path 分流）：仍用 CF_HOSTNAME，但在 Tunnel ingress 里
        按 path 拆：CF_WS_PATH 回源到 28080（WS 入站），其余（含 SUB_PATH）回源到 80（Nginx），
        订阅地址用 https://CF_HOSTNAMESUB_PATH
```

两种方式的 Tunnel 配置见下面「cloudflared」一节，已分别给出 ingress 模板。

无论 A 还是 B，cloudflared 回源到本机 80 时带的 Host（A 是 `CF_SUB_HOSTNAME`，B 是 `CF_HOSTNAME`）必须出现在 Nginx 订阅 server 的 `server_name` 里，否则会被默认 server 404。部署时把这个 Host 加进上一节 `server_name SUB_DOMAIN_1 SUB_DOMAIN_2 ...` 那行。

默认不建议把 Nginx 直接监听公网 `443`：443 通常已给 VLESS Reality，直接抢端口会让节点失效。

但如果需要标准的 `https://订阅域名/随机路径`，可按下一节的 **Nginx 443 SNI 分流**实施：公网 443 仍只由 Nginx 的 `stream` 前端监听；它按 TLS SNI 把订阅域名送往本机 HTTPS 看板，把 Reality 的伪装 SNI 送回 Xray/sing-box 本机端口。这个是经过实测的例外方案，不是让两个进程同时抢 443。

### 直连 HTTPS：Nginx 443 SNI 分流（已验证方案）

适用目标：不依赖 Cloudflare 橙云或 Tunnel，让看板和订阅使用普通 HTTPS，同时保持 Hysteria2 / AnyTLS / VLESS Reality / SS2022 的节点地址和对外端口不变。

最终流向：

```text
浏览器 / Clash
  -> https://SUB_DOMAIN_1/SUB_PATH (TCP 443, SNI=SUB_DOMAIN_1)
  -> Nginx stream 443
  -> Nginx TLS 后端 127.0.0.1:2053
  -> 看板 / Clash YAML / Base64 订阅

VLESS Reality 客户端
  -> SERVER_IP:443 (SNI=REALITY_SNI，例如 www.bing.com)
  -> Nginx stream 443
  -> Xray 或 sing-box 的 127.0.0.1:1443
```

关键边界：

```text
订阅和看板使用 SUB_DOMAIN_1 的 HTTPS 域名。
订阅 YAML 内四协议节点的 server: 仍写 VPS 直连 IP；不要改成 SUB_DOMAIN_1。
Hysteria2 4433/udp、AnyTLS 4434/tcp、SS2022 4435/tcp+udp 均不改。
VLESS Reality 对外仍是 SERVER_IP:443；只是 Xray/sing-box 的实际监听移到 127.0.0.1:1443。
公网 443 只由 Nginx stream 持有，Xray/sing-box 不能再监听公网 443。
```

DNS 先设为直连（Cloudflare `DNS only` / 灰云）：

```text
SUB_DOMAIN_1  A  SERVER_IP  DNS only
```

> 建议先用灰云验证本方案，故障面最小。若要保留 Cloudflare 橙云，公网 HTTPS 仍会回源 VPS 的标准 443；只要本节的 SNI 分流已验证成功，并将 Cloudflare SSL/TLS 模式设为 `Full (strict)`，即可正常使用，不需要 Origin Rule。不要用 Flexible；它会让 Cloudflare 到源站退化为 HTTP。若改用非标准回源端口，才需要另配 Origin Rule。

#### 1. 申请可自动续期的证书

使用 Cloudflare DNS-01 验证，不依赖 80 端口，因此适合安全组受限的 VPS。Cloudflare API token 只需要 `Zone > DNS > Edit`，凭据文件必须 600：

```bash
apt update
apt install -y certbot python3-certbot-dns-cloudflare libnginx-mod-stream

install -d -m 700 /root/.secrets
cat >/root/.secrets/cloudflare-dns.ini <<'EOF'
dns_cloudflare_api_token = CLOUDFLARE_DNS_API_TOKEN
EOF
chmod 600 /root/.secrets/cloudflare-dns.ini

certbot certonly --non-interactive --agree-tos --register-unsafely-without-email \
  --dns-cloudflare \
  --dns-cloudflare-credentials /root/.secrets/cloudflare-dns.ini \
  --dns-cloudflare-propagation-seconds 60 \
  --cert-name SUB_DOMAIN_1 \
  -d SUB_DOMAIN_1

install -d -m 755 /etc/letsencrypt/renewal-hooks/deploy
cat >/etc/letsencrypt/renewal-hooks/deploy/reload-nginx <<'EOF'
#!/bin/sh
systemctl reload nginx
EOF
chmod 755 /etc/letsencrypt/renewal-hooks/deploy/reload-nginx
systemctl enable --now certbot.timer
```

证书验收：

```bash
openssl x509 -in /etc/letsencrypt/live/SUB_DOMAIN_1/fullchain.pem -noout -subject -enddate
certbot renew --dry-run
```

#### 2. 配置仅本机监听的 HTTPS 看板后端

创建 `/etc/nginx/conf.d/90-subscription-https.conf`。`2053` 只作为 Nginx 内部 SNI 分流后端，**必须**监听 `127.0.0.1`，不要在云安全组或 UFW 开放它：

```nginx
server {
    listen 127.0.0.1:2053 ssl;
    server_name SUB_DOMAIN_1;

    root /var/www/html;
    ssl_certificate /etc/letsencrypt/live/SUB_DOMAIN_1/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/SUB_DOMAIN_1/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    add_header Strict-Transport-Security "max-age=31536000" always;

    location = SUB_PATH {
        include /etc/nginx/snippets/sub_headers.conf;
        default_type application/octet-stream;
        try_files $uri =404;
    }

    # 有通用 Base64 订阅才保留这一段。
    location = SUB_B64_PATH {
        include /etc/nginx/snippets/sub_headers.conf;
        default_type text/plain;
        try_files $uri =404;
    }

    # 下面两个 PANEL location 只对【用一键脚本装过看板】的机器有意义。
    # 纯手搓主线没有看板页、也没有 $sb_ok 这个认证 map，照抄会 nginx -t 报 unknown variable $sb_ok
    # ——这种机器直接删掉这两个 location 即可（订阅/Base64 两段保留）。
    location = PANEL_PATH {
        # 一键脚本的看板认证 map 变量通常为 $sb_ok；旧兼容看板可能是 $codex_panel_ok。
        # 按当前 HTTP 看板的 location 原样复制认证判断，不要猜变量名。
        if ($sb_ok = 0) { return 302 PANEL_LOGIN_PATH; }
        default_type text/html;
        try_files $uri =404;
    }

    location = PANEL_LOGIN_PATH {
        default_type text/html;
        try_files $uri =404;
    }

    location / { return 404; }
}
```

替换说明：

```text
SUB_PATH         真实 Clash/Mihomo 随机路径，例如 /sub-xxxx.yaml
SUB_B64_PATH     真实 Base64 随机路径；没有则删除整个 location
PANEL_PATH       真实看板路径，例如 /panel-xxxx.html
PANEL_LOGIN_PATH 真实登录路径，例如 /panel-xxxx-login.html
$sb_ok           当前一键脚本看板认证变量
$codex_panel_ok  旧版兼容看板认证变量
```

先检查本机后端，不要急着动 Reality：

```bash
nginx -t && systemctl reload nginx
ss -lntp | grep 2053
openssl s_client -connect 127.0.0.1:2053 -servername SUB_DOMAIN_1 -brief </dev/null
```

预期：`2053` 只显示 `127.0.0.1:2053`，证书 CN 是 `SUB_DOMAIN_1`。

#### 3. 给 Nginx 加 stream SNI 前端

`stream` 必须在 Nginx 的顶层，不能写进 `http {}`。Debian/Ubuntu 包的模块和 include：

```bash
apt install -y libnginx-mod-stream
mkdir -p /etc/nginx/stream.d

# 仅在 nginx.conf 不含该行时添加；位置在 http { 之前。
grep -Fq 'include /etc/nginx/stream.d/*.conf;' /etc/nginx/nginx.conf || \
  sed -i '/^http {/i include /etc/nginx/stream.d/*.conf;' /etc/nginx/nginx.conf
```

创建 `/etc/nginx/stream.d/90-subscription-https.conf`。Reality SNI 必须与服务器实际配置完全一致：

```nginx
stream {
    map $ssl_preread_server_name $subscription_stream_backend {
        hostnames;
        .SUB_DOMAIN_1 127.0.0.1:2053;
        REALITY_SNI   127.0.0.1:1443;
        default       127.0.0.1:1443;
    }

    server {
        listen 443;
        # 有 IPv6 并且需要 IPv6 访问时，再增加 listen [::]:443;
        proxy_pass $subscription_stream_backend;
        ssl_preread on;
    }
}
```

`default` 必须回到 Reality 后端，不能指向订阅 HTTPS 后端；否则直接以 IP 使用 VLESS 的客户端会被误送到网页服务。

> **已有 stream/443 SNI 路由的机器不能照抄覆盖。** 例如机器已经用 `map $ssl_preread_server_name ...` 将 Komari、其它网站或 Reality 送往不同本机端口时：保留原有 `stream {}`、`map`、`upstream` 和 `server { listen 443; }`，只在同一个现有 `map` 内新增 `SUB_DOMAIN_1 -> 127.0.0.1:2053`。不要新建第二个 `listen 443`，也不要嵌套第二个 `stream {}`；两者都会导致 Nginx 配置失败或覆盖已有路由。

#### 4. 将 Reality 后端移到本机 1443

先完整备份。此操作会让 VLESS Reality 有数秒重连窗口，但不会改 HY2 / AnyTLS / SS2022：

```bash
ts=$(date +%Y%m%d-%H%M%S)
mkdir -p /root/vps-config-backups/$ts
```

**Xray Reality：**

```bash
cp -a /usr/local/etc/xray/config.json /root/vps-config-backups/$ts/xray-config.json
tmp=/tmp/xray-https-$ts.json
jq '(.inbounds[] | select(.tag == "vless-reality-in")) |= (.listen = "127.0.0.1" | .port = 1443)' \
  /usr/local/etc/xray/config.json >"$tmp"
xray run -test -config "$tmp"
install -o root -g root -m 600 "$tmp" /usr/local/etc/xray/config.json
rm -f "$tmp"
systemctl restart xray
```

**sing-box Reality：**

```bash
cp -a /etc/sing-box/config.json /root/vps-config-backups/$ts/sing-box-config.json
tmp=/tmp/sing-box-https-$ts.json
jq '(.inbounds[] | select(.tag == "vless-in")) |= (.listen = "127.0.0.1" | .listen_port = 1443)' \
  /etc/sing-box/config.json >"$tmp"
sing-box check -c "$tmp"
install -o root -g root -m 600 "$tmp" /etc/sing-box/config.json
rm -f "$tmp"
systemctl restart sing-box
```

然后加载 Nginx stream：

```bash
nginx -t && systemctl restart nginx
ss -lntp | grep -E ':(443|1443|2053)\b'
```

预期：

```text
0.0.0.0:443       nginx stream
127.0.0.1:1443   xray 或 sing-box Reality
127.0.0.1:2053   nginx HTTPS 看板后端
```

#### 5. 更新看板与客户端订阅 URL

看板 HTML、二维码和一键导入链接是静态生成的。必须将其中的订阅 URL 从：

```text
http://旧IP/SUB_PATH
```

改成：

```text
https://SUB_DOMAIN_1/SUB_PATH
```

Base64 订阅、一键导入深链、二维码、页面内的 `fetch()` 延迟测试也要同步更新。**不要**替换下方单节点分享链接中的 `SERVER_IP:443` / `4433` / `4434` / `4435`，那些必须保持直连节点地址。

可用一键脚本的新版 `panel` 重新渲染；旧部署没有保存 `SUB_HOST` 时，直接替换看板中仅用于订阅的 `http://旧IP/` 即可，改前先备份：

```bash
cp -a /var/www/htmlPANEL_PATH /var/www/htmlPANEL_PATH.bak-before-https
sed -i 's|http://SERVER_IP/|https://SUB_DOMAIN_1/|g' /var/www/htmlPANEL_PATH
```

#### 6. 验收与回滚

从另一台 VPS 或本地做 SNI 验证，不能只看本机服务是否 active：

```bash
openssl s_client -connect SERVER_IP:443 -servername SUB_DOMAIN_1 \
  -verify_return_error -CAfile /etc/ssl/certs/ca-certificates.crt -brief </dev/null

curl --noproxy '*' --resolve "SUB_DOMAIN_1:443:SERVER_IP" \
  -I "https://SUB_DOMAIN_1PANEL_LOGIN_PATH"

# Reality 伪装 SNI 仍必须能到对应后端。
openssl s_client -connect SERVER_IP:443 -servername REALITY_SNI -brief </dev/null
```

验收标准：

```text
SUB_DOMAIN_1 的证书 CN/SAN 正确，验证显示 Verification: OK。
HTTPS 看板登录页、Clash 订阅、Base64 订阅均返回 HTTP 200。
订阅响应仍带 Subscription-Userinfo。
Nginx、Xray/sing-box 均 active。
HY2 4433/udp、AnyTLS 4434/tcp、SS2022 4435 仍在监听。
Reality SNI 仍能完成 TLS 握手。
```

回滚顺序：删除 `/etc/nginx/stream.d/90-subscription-https.conf`，恢复备份的 Xray/sing-box 配置使其重新监听公网 443，恢复原 `nginx.conf`（若新增了 stream include），然后依次执行：

```bash
systemctl restart xray      # 或 systemctl restart sing-box
nginx -t && systemctl restart nginx
```

不要执行 `systemctl restart nginx` 前删除原代理配置；否则公网 443 会短暂没有 Reality 后端。

## Nginx 订阅服务

订阅头片段由脚本生成，初始值从 `/etc/sing-box-node.env` 读，不再硬编码：

```bash
mkdir -p /etc/nginx/snippets

# 用 env 里的单一来源生成初始头（download=0 占位，随后由 traffic_limit.py 刷新）
python3 - <<'PY'
import datetime
env = {}
for line in open("/etc/sing-box-node.env"):
    line = line.strip()
    if line and not line.startswith("#") and "=" in line:
        k, v = line.split("=", 1)
        env[k.strip()] = v.strip().strip('"').strip("'")
limit = int(float(env.get("LIMIT_GB", "200")) * 1024 ** 3)
# EXPIRE_AT 缺失或为空时回退 expire=0，和 traffic_limit.py 保持一致
expire_raw = env.get("EXPIRE_AT")
exp = int(datetime.datetime.strptime(expire_raw, "%Y-%m-%d %H:%M:%S %z").timestamp()) if expire_raw else 0
with open("/etc/nginx/snippets/sub_headers.conf", "w") as f:
    f.write(f'add_header Subscription-Userinfo "upload=0; download=0; total={limit}; expire={exp}" always;\n')
PY

chown root:root /etc/nginx/snippets/sub_headers.conf
chmod 644 /etc/nginx/snippets/sub_headers.conf
```

不要把流量头放到：

```text
/etc/nginx/conf.d/sub_headers.conf
```

原因是 Ubuntu Nginx 默认会全局加载 `conf.d/*.conf`，那样首页和 404 也会带 `Subscription-Userinfo`。

配置 Nginx：

拆成两个 server：默认 server 吃掉所有「Host 不对」或「直接用 IP 访问」的请求并一律 404；订阅 server 只绑定订阅域名，且只在精确的 `SUB_PATH` 上输出内容。这样即使有人猜中路径，用错 Host 或直接打 IP 也拿不到订阅。

```bash
cat >/etc/nginx/sites-enabled/default <<'EOF'
# 默认 server：兜底所有未匹配 Host（含直接用 IP 访问），一律 404，不暴露任何内容
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 404;
}

# 订阅 server：只对订阅域名生效，只在精确订阅路径上输出
server {
    listen 80;
    listen [::]:80;
    server_name SUB_DOMAIN_1 SUB_DOMAIN_2;

    root /var/www/html;

    location = SUB_PATH {
        include /etc/nginx/snippets/sub_headers.conf;
        default_type application/octet-stream;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
EOF
```

注意：

- 上面 `location = SUB_PATH` 要替换成真实随机路径，例如 `location = /sub-a8f3c91d7e4b4c6f.yaml {`。这一步最容易漏，漏了订阅会 404 而 `nginx -t` 仍然通过。检查段有一条专门验证它有没有替换干净。
- `server_name SUB_DOMAIN_1 SUB_DOMAIN_2` 也要替换成真实域名。如果**没有**第二个订阅域名，删掉 `SUB_DOMAIN_2`，只留 `server_name SUB_DOMAIN_1;`。
- 这个 heredoc 用单引号 `<<'EOF'` 是**故意的**：块里有 nginx 自己的变量 `$uri`，不能让 shell 展开。所以这里不能改成展开式 heredoc，`SUB_PATH` / 域名用手工替换。
- 如果 VPS 没有 IPv6，删掉两处 `listen [::]:80 ...;`，否则在 IPv6 被禁用的内核上 `nginx -t` 会报 `Address family not supported`。
- 用 Cloudflare Tunnel 走加密订阅时（见「订阅传输安全」），cloudflared 会带上你配置的订阅 Host 回源到本机 80，正好命中这个订阅 server；如果改用一个不在 `server_name` 里的 Host 回源，会被默认 server 404，记得把那个 Host 也加进 `server_name`。

关闭 Nginx 版本暴露：

```bash
if grep -q '^[[:space:]]*server_tokens ' /etc/nginx/nginx.conf; then
  sed -i 's/^[[:space:]]*server_tokens .*/    server_tokens off;/' /etc/nginx/nginx.conf
else
  sed -i '/^[[:space:]]*http[[:space:]]*{/a\    server_tokens off;' /etc/nginx/nginx.conf
fi
```

检查并重载：

```bash
nginx -t
systemctl reload nginx
```

## CF-Vless-WS 保底部署（即 Argo 隧道节点）

这一节就是俗称的「Argo 隧道节点」——用 `cloudflared`（Cloudflare/Argo 隧道）把一条 VLESS-WS 接出去。它是「大保底」用的，不替代前面的四条主力节点。

### sing-box 本地入站

在 VPS 上新增一个只监听本机的 VLESS WebSocket 入站。**它要作为 `inbounds` 数组里的第 5 个元素加进去**，在最后那个 `ss-in` 对象后面补一个逗号再追加（`ss-in` 是四协议里的最后一个入站），不要替换整个文件：

```json
{
  "type": "vless",
  "tag": "cf-vless-ws-in",
  "listen": "127.0.0.1",
  "listen_port": 28080,
  "users": [
    {
      "uuid": "CF_VLESS_UUID"
    }
  ],
  "transport": {
    "type": "ws",
    "path": "CF_WS_PATH"
  }
}
```

加完一定要重新校验，逗号漏了会让整个 config 解析失败：

```bash
sing-box check -c /etc/sing-box/config.json
systemctl restart sing-box
```

推荐变量：

```text
# 下面两个是模板已写死的默认值（127.0.0.1:28080），保留即可，正文没有同名 token 可替换。
# 如果要改，必须同步改 sing-box 的 listen/listen_port 和 cloudflared 的回源地址。
CF_WS_LISTEN = 127.0.0.1
CF_WS_PORT = 28080

# 下面这些才是 CF-Vless 章节需要替换 / 填写的变量。
CF_WS_PATH = /cf-vless-随机字符串
CF_HOSTNAME = cf-vps.example.com
CF_VLESS_UUID = sing-box generate uuid（可与主 VLESS 不同）
CF_SUB_HOSTNAME = cf-sub.example.com   # 仅「订阅传输安全」方式 A 用：订阅专用域名，回源到本机 80。方式 B / 不走 CF 订阅可不填
```

为什么只监听 `127.0.0.1`：

```text
这个入站只给 cloudflared 本机转发使用
不直接暴露公网端口
减少被扫到的概率
```

### cloudflared

安装：

```bash
# 极简镜像可能没有 keyrings 目录，先建好再写入
mkdir -p --mode=0755 /usr/share/keyrings

curl -fsSL https://pkg.cloudflare.com/cloudflare-main.gpg \
  | tee /usr/share/keyrings/cloudflare-main.gpg >/dev/null

echo "deb [signed-by=/usr/share/keyrings/cloudflare-main.gpg] https://pkg.cloudflare.com/cloudflared any main" \
  > /etc/apt/sources.list.d/cloudflared.list

apt update
apt install -y cloudflared
cloudflared --version
```

Cloudflare Tunnel 有两种常用绑定方式，**二选一，不要两种都做**。两种方式都会装/管同一个 `cloudflared` 服务，混用会互相覆盖配置、产生冲突。选定一种后，跳过另一种的全部步骤。

```text
方式一 = Dashboard 建 Tunnel + token 安装（cloudflared service install）。配置在 Cloudflare 云端，VPS 上不写 config.yml。
方式二 = 本机 cloudflared tunnel login/create + 自己写 config.yml + 自建 systemd。配置在 VPS 本地。
两者都用 token / 都装一遍，会让 cloudflared 服务定义冲突，二选一即可。
```

方式一：Cloudflare Dashboard 创建 Tunnel，复制 token 后在 VPS 上安装服务。WS 大保底只需要第一条 public hostname：

```text
Hostname: CF_HOSTNAME
Service:  http://127.0.0.1:28080
```

如果还要用「订阅传输安全」的 Cloudflare 加密订阅，在 Dashboard 里**再加一条 public hostname**：

```text
方式 A（推荐，另起订阅专用域名）：
  Hostname: CF_SUB_HOSTNAME
  Service:  http://127.0.0.1:80
  # 并在 Nginx 订阅 server 的 server_name 里加上 CF_SUB_HOSTNAME

方式 B（共用 CF_HOSTNAME，按 path 分流，注意顺序：精确的 WS path 在前）：
  Hostname: CF_HOSTNAME  Path: CF_WS_PATH   Service: http://127.0.0.1:28080
  Hostname: CF_HOSTNAME（不填 Path，兜底）  Service: http://127.0.0.1:80
  # 并在 Nginx 订阅 server 的 server_name 里加上 CF_HOSTNAME
```

```bash
cloudflared service install CLOUDFLARE_TUNNEL_TOKEN
systemctl enable --now cloudflared
systemctl is-active cloudflared
```

Token 方式是最快的，但它的配置主要在 Cloudflare 云端，VPS 本地不好精细控制。低内存、丢包高、QUIC 不稳的机器，更推荐下面“方式二”本机 `config.yml`，可以明确写 `protocol: http2` 和 `loglevel: warn`。

如果已经用了 token 方式，至少把 systemd 服务改成更宽松的启动方式，避免 `Type=notify` + 过短超时导致服务反复启动失败：

```bash
cp -a /etc/systemd/system/cloudflared.service \
  /etc/systemd/system/cloudflared.service.bak-$(date +%Y%m%d-%H%M%S)

# 不打印 token，只改 service 类型和启动超时。
grep -q '^Type=' /etc/systemd/system/cloudflared.service \
  && sed -i 's/^Type=.*/Type=simple/' /etc/systemd/system/cloudflared.service \
  || sed -i '/^\[Service\]/a Type=simple' /etc/systemd/system/cloudflared.service

grep -q '^TimeoutStartSec=' /etc/systemd/system/cloudflared.service \
  && sed -i 's/^TimeoutStartSec=.*/TimeoutStartSec=60/' /etc/systemd/system/cloudflared.service \
  || sed -i '/^\[Service\]/a TimeoutStartSec=60' /etc/systemd/system/cloudflared.service

systemctl daemon-reload
systemctl restart cloudflared
systemctl status cloudflared --no-pager -l | sed -E 's/--token [A-Za-z0-9._=-]+/--token <hidden>/g'
```

方式二：在 VPS 上用 Cloudflare 登录证书创建命名 Tunnel：

```bash
cloudflared tunnel login
cloudflared tunnel create TUNNEL_NAME
# 上一条会打印 "Created tunnel TUNNEL_NAME with id <UUID>"，并把凭证写到
# ~/.cloudflared/<UUID>.json（root 执行就是 /root/.cloudflared/）。
# 记下这个 <UUID>，下面所有 TUNNEL_ID 占位符都替换成它。

# 把凭证挪到 /etc/cloudflared/，否则 systemd（User=root）按 config 里的
# /etc/cloudflared/TUNNEL_ID.json 找不到凭证、服务起不来。
mkdir -p /etc/cloudflared
cp /root/.cloudflared/TUNNEL_ID.json /etc/cloudflared/TUNNEL_ID.json
chmod 600 /etc/cloudflared/TUNNEL_ID.json
```

创建 `/etc/cloudflared/config.yml`。基础版（只做 WS 大保底，不走 Cloudflare 加密订阅）：

```yaml
tunnel: TUNNEL_ID
credentials-file: /etc/cloudflared/TUNNEL_ID.json
protocol: http2
# 低配 / 丢包 / UDP 不稳机器优先 http2：牺牲一点峰值，换更稳的 Tunnel。
# 如果确认机房到 Cloudflare QUIC 很稳，再改回 auto 或 quic 观察。
loglevel: warn
ingress:
  - hostname: CF_HOSTNAME
    service: http://127.0.0.1:28080
  - service: http_status:404
```

如果同时要走「订阅传输安全」的 Cloudflare 加密订阅，二选一替换上面的 `ingress`：

```yaml
# 方式 A（推荐）：订阅走单独域名 CF_SUB_HOSTNAME，回源到本机 Nginx(80)
ingress:
  - hostname: CF_HOSTNAME          # WS 大保底
    service: http://127.0.0.1:28080
  - hostname: CF_SUB_HOSTNAME      # 加密订阅
    service: http://127.0.0.1:80
  - service: http_status:404
```

```yaml
# 方式 B：共用 CF_HOSTNAME，按 path 分流。精确的 WS path 规则必须排在兜底规则前面，
#         否则订阅兜底会把 WS 流量也吞掉。
ingress:
  - hostname: CF_HOSTNAME
    path: CF_WS_PATH               # 只有 WS 路径走 28080
    service: http://127.0.0.1:28080
  - hostname: CF_HOSTNAME          # 其余（含 SUB_PATH）走 Nginx(80)
    service: http://127.0.0.1:80
  - service: http_status:404
```

> 别忘了把回源到 80 用的那个 Host（方式 A 是 `CF_SUB_HOSTNAME`，方式 B 是 `CF_HOSTNAME`）加进 Nginx 订阅 server 的 `server_name`，否则会被默认 server 404。

把 DNS 记录绑定到 Tunnel（方式 A 两个域名都要绑）：

```bash
cloudflared tunnel route dns --overwrite-dns TUNNEL_NAME CF_HOSTNAME
# 方式 A 额外执行：
cloudflared tunnel route dns --overwrite-dns TUNNEL_NAME CF_SUB_HOSTNAME
```

创建 systemd 服务。注意 `cloudflared` 的可执行路径随安装方式不同——`apt install cloudflared` 通常装到 `/usr/bin/cloudflared`，不是 `/usr/local/bin/`。所以下面用 `command -v` 自动定位，不要硬编码路径：

```bash
# 1. 先校验 ingress 配置合法（path/service 写错会在这里直接报出来，不用等服务起不来才发现）
cloudflared tunnel --config /etc/cloudflared/config.yml ingress validate

# 2. 自动定位 cloudflared 实际路径
CFD=$(command -v cloudflared)
echo "cloudflared 路径: $CFD"   # 应为非空，通常是 /usr/bin/cloudflared

# 3. 用定位到的路径生成 service 文件（unquoted EOF，让 ${CFD} 展开）
cat >/etc/systemd/system/cloudflared.service <<EOF
[Unit]
Description=Cloudflare Tunnel
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
TimeoutStartSec=60
User=root
ExecStart=${CFD} --no-autoupdate --config /etc/cloudflared/config.yml tunnel run
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
EOF
```

启动：

```bash
systemctl daemon-reload
systemctl enable --now cloudflared
systemctl is-active cloudflared
```

> `TUNNEL_ID.json` 凭证只在 `cloudflared tunnel create` 时生成一次，无法重新下载。丢了只能重建 Tunnel。记得纳入备份（见“修改前备份”），且不要外传。

不管用 token 方式还是命名 Tunnel 方式，都要等下面「验证」部分看到 `101 Switching Protocols`、确认 `CF_HOSTNAME` 公网可达之后，才把 `CF-Vless` 加入订阅；提前加就是死节点（这条规则全文以本节为准）。

### 客户端节点模板

公网验证通过后，把这个节点加入订阅：

```yaml
  - name: "CF-SERVER-WS"
    type: vless
    server: CF_HOSTNAME
    port: 443
    uuid: CF_VLESS_UUID
    network: ws
    udp: true
    tls: true
    servername: CF_HOSTNAME
    client-fingerprint: chrome
    ws-opts:
      path: CF_WS_PATH
      headers:
        Host: CF_HOSTNAME
```

代理组里放最后：

```yaml
proxy-groups:
  - name: "🚀 节点选择"
    type: select
    proxies:
      - "Hysteria2"
      - "AnyTLS"
      - "Vless"
      - "SS2022"
      - "CF-SERVER-WS"
```

验证：

```bash
# 先测 VPS 本机 WS 入站。这里能 101，说明 sing-box 的 127.0.0.1:28080 是活的。
curl -i --http1.1 \
  -H "Host: CF_HOSTNAME" \
  -H "Connection: Upgrade" \
  -H "Upgrade: websocket" \
  -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
  -H "Sec-WebSocket-Version: 13" \
  http://127.0.0.1:28080CF_WS_PATH

# 再测公网 Cloudflare 域名。这里能 101，才说明 Tunnel + DNS + ingress 全通。
curl -i \
  -H "Connection: Upgrade" \
  -H "Upgrade: websocket" \
  -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
  -H "Sec-WebSocket-Version: 13" \
  https://CF_HOSTNAMECF_WS_PATH

systemctl is-active cloudflared sing-box
```

看到 `101 Switching Protocols`，说明 Cloudflare 已经能打到 VPS 本机的 WebSocket 入站。

如果本机 `127.0.0.1:28080` 能 `101`，公网 `CF_HOSTNAME` 返回 `502` / `530` / `1033`，问题在 Cloudflare Tunnel / DNS / cloudflared，不在 sing-box 主线四协议。先看：

```bash
systemctl status cloudflared --no-pager -l | sed -E 's/--token [A-Za-z0-9._=-]+/--token <hidden>/g'
journalctl -u cloudflared -n 100 --no-pager | grep -Ei 'Registered|timeout|failed|error|quic|http2'
```

再用客户端或临时 sing-box 客户端测一次代理链路，确认能正常访问外网后，再加入常用订阅。

## 流量显示和限流

Clash/Mihomo 显示流量靠 HTTP 响应头，不是靠 YAML 内容：

```http
Subscription-Userinfo: upload=0; download=已用字节; total=总量字节; expire=到期时间戳
```

脚本设计要点：

```text
所有参数从 /etc/sing-box-node.env 读，不在脚本里硬编码
按 COUNT_MODE 统计（默认 tx 只算出站，匹配多数商家；代理 rx≈tx，rx+tx 会翻倍误算）
   —— vnstat 统计的是整块网卡流量，SSH、系统更新等也会算进去；偏保守、更安全
只有头内容变化时才写文件并 reload nginx；流量持续增长时每轮 reload 属正常
   —— 因为 Subscription-Userinfo 是 nginx 配置 include，文件变化后需要 reload 才生效
超额停 sing-box 时在 /var/lib/sing-box-node/ 打一个持久标记；只有带标记才会在额度恢复后自动拉起
   —— 手动 systemctl stop sing-box（无标记）不会被定时任务复活
想永久停用这台节点，不再受配额自动恢复影响：
   systemctl disable --now sing-box && rm -f /var/lib/sing-box-node/quota-stopped
网卡找不到时报错到 stderr（cron 已接到 logger，可在 journal 看到），不会静默
```

创建脚本：

```bash
nano /usr/local/bin/traffic_limit.py
```

模板：

```python
#!/usr/bin/env python3
import json
import subprocess
import datetime
import sys
import os

ENV_PATH = "/etc/sing-box-node.env"
HEADER_PATH = "/etc/nginx/snippets/sub_headers.conf"
STATE_DIR = "/var/lib/sing-box-node"
QUOTA_FLAG = os.path.join(STATE_DIR, "quota-stopped")


def load_env(path=ENV_PATH):
    env = {}
    with open(path) as f:
        for line in f:
            line = line.strip()
            if not line or line.startswith("#") or "=" not in line:
                continue
            k, v = line.split("=", 1)
            env[k.strip()] = v.strip().strip('"').strip("'")
    return env


def parse_expire(s):
    return int(datetime.datetime.strptime(s, "%Y-%m-%d %H:%M:%S %z").timestamp())


def current_month_used(data, interface, mode, now=None):
    """返回当前账期已用字节；接口不存在返回 None。"""
    now = now or datetime.datetime.now()
    iface = None
    for it in data.get("interfaces", []):
        if it.get("name") == interface:
            iface = it
            break
    if iface is None:
        return None
    months = iface.get("traffic", {}).get("month", [])
    if not months:
        return 0
    # 优先取标签等于当前自然月的桶；匹配不到（例如 vnstat 配了 MonthRotate、
    # 账期跨入新自然月的前半段）就退回最后一个桶。vnstat 按时间升序输出，
    # 最后一个桶就是当前账期，对自然月和 MonthRotate 都成立。
    cur = months[-1]
    for m in months:
        d = m.get("date", {})
        if d.get("year") == now.year and d.get("month") == now.month:
            cur = m
            break
    rx = cur.get("rx", 0)
    tx = cur.get("tx", 0)
    if mode == "rx+tx":
        return rx + tx  # 仅商家真按"进+出"计费才用；代理 rx≈tx，这是真实用量的约 2 倍
    if mode == "max":
        return max(rx, tx)
    return tx  # 默认 tx：只算出站，匹配绝大多数商家


def build_header(used, total, expire):
    return (f'add_header Subscription-Userinfo '
            f'"upload=0; download={used}; total={total}; expire={expire}" always;\n')


def decide_enforcement(used, limit_bytes, active, flag_exists):
    """返回 (action, keep_flag)；action in {None,'stop','start'}。"""
    if used >= limit_bytes:
        if active:
            return ("stop", True)        # 超额且在跑：停掉并打配额标记
        # 已经停了：只有原本就是配额停（有标记）才保留标记；
        # 手动停（无标记）不抢标记，否则下月恢复会被误当配额停机拉起
        return (None, flag_exists)
    if flag_exists:
        return ("start" if not active else None, False)
    return (None, False)  # 无标记的手动停机：不动


def main():
    os.makedirs(STATE_DIR, exist_ok=True)
    env = load_env()
    limit_bytes = int(float(env.get("LIMIT_GB", "200")) * 1024 ** 3)
    interface = env.get("INTERFACE", "eth0")
    mode = env.get("COUNT_MODE", "tx")
    # EXPIRE_AT 缺失/为空时不崩溃，回退 expire=0（多数客户端视为"无到期"）
    expire_raw = env.get("EXPIRE_AT")
    expire = parse_expire(expire_raw) if expire_raw else 0

    try:
        result = subprocess.run(["vnstat", "--json"], capture_output=True, text=True, check=True)
        data = json.loads(result.stdout)
    except Exception as e:
        print(f"Error running vnstat: {e}", file=sys.stderr)
        sys.exit(1)

    used = current_month_used(data, interface, mode)
    if used is None:
        print(f"Interface {interface} not found in vnstat data", file=sys.stderr)
        sys.exit(1)

    header = build_header(used, limit_bytes, expire)
    old = ""
    if os.path.exists(HEADER_PATH):
        with open(HEADER_PATH) as f:
            old = f.read()
    if header != old:
        with open(HEADER_PATH, "w") as f:
            f.write(header)
        subprocess.run(["systemctl", "reload", "nginx"])

    active = subprocess.run(["systemctl", "is-active", "--quiet", "sing-box"]).returncode == 0
    action, keep_flag = decide_enforcement(used, limit_bytes, active, os.path.exists(QUOTA_FLAG))
    if action == "stop":
        subprocess.run(["systemctl", "stop", "sing-box"])
    elif action == "start":
        subprocess.run(["systemctl", "start", "sing-box"])
    if keep_flag:
        open(QUOTA_FLAG, "w").close()
    elif os.path.exists(QUOTA_FLAG):
        os.remove(QUOTA_FLAG)


if __name__ == "__main__":
    main()
```

启用：

```bash
chmod 700 /usr/local/bin/traffic_limit.py
python3 -m py_compile /usr/local/bin/traffic_limit.py

# 前台跑一次：网卡名写错会在这里立刻报 "Interface ... not found"，不会被定时任务隐藏
# 刚装 vnstat 后如果同样报这个错，可能是 vnstatd 还没采到网卡；等 1-2 分钟再跑。
# 仍然没有时，先手动把 env 里的网卡加进 vnstat：
# . /etc/sing-box-node.env
# vnstat --add -i "$INTERFACE" 2>/dev/null || true
# systemctl restart vnstat
python3 /usr/local/bin/traffic_limit.py

# stderr 接到 logger，失败可用 journalctl -t traffic_limit 查看
cat >/etc/cron.d/traffic_limit <<'EOF'
*/5 * * * * root /usr/bin/python3 /usr/local/bin/traffic_limit.py 2>&1 | /usr/bin/logger -t traffic_limit
EOF
```

> 月度账期：默认按自然月统计。服务商按“开通日”这种非自然月账期计费时，在 vnstat 配置里设置 `MonthRotate`（1–28）对齐账期即可——脚本会优先匹配当前自然月的桶，匹配不到时自动退回 vnstat 最新账期桶，所以配了 MonthRotate 也能正确统计（旧版脚本在账期前半段会把用量误算成 0、并可能把配额停机的节点误拉起，已修复）。

## DNS 和客户端

DNS（先看机器是哪种栈，再决定配哪种记录，详见后面「IPv4 / IPv6 / 双栈」）：

```text
IPv4-only（最常见）：只配 A
  SUB_DOMAIN_1  A     SERVER_IP
  SUB_DOMAIN_2  A     SERVER_IP

IPv6-only：只配 AAAA（前提：客户端网络要有 IPv6）
  SUB_DOMAIN_1  AAAA  SERVER_IPV6
  SUB_DOMAIN_2  AAAA  SERVER_IPV6

双栈：按需配；建议拆两个域名各配一种，避免客户端乱选 IPv6
  node4.example.com  A     SERVER_IP
  node6.example.com  AAAA  SERVER_IPV6
```

如果 DNS 在 Cloudflare 托管，当前直连节点域名建议先用灰云 `DNS only`。

### Cloudflare 橙云只适合订阅网页，不等于代理四协议节点

如果某个域名只是给 Nginx 订阅网页用，可以开 Cloudflare 橙云 `Proxied`，让客户端拉订阅时走 Cloudflare：

```text
客户端拉订阅
  -> Cloudflare 橙云
  -> VPS 的 Nginx:80
  -> 返回订阅 YAML / base64 / 看板页
```

但订阅 YAML 里面每个节点的 `server:` 仍然要写 VPS 直连地址，例如：

```text
Type: A
Name: sub
IPv4 address: SERVER_IP
Proxy status: Proxied（橙云，只用于订阅网页时才这样）

订阅 URL: http://sub.example.comSUB_PATH
# 如果已用 Cloudflare Tunnel / 合适的 SSL 回源方式让橙云命中 Nginx:80，
# 才把订阅 URL 写成 https://sub.example.comSUB_PATH。
节点 server: SERVER_IP      # 或灰云直连域名；不要写橙云域名
```

这时“走 Cloudflare 代理”的只有**拉订阅这一段 HTTP/HTTPS 请求**，不是节点流量。客户端真正连接节点时仍然按订阅里的 `server:` 去连 VPS：

```text
Hysteria2 / Reality / AnyTLS / SS2022
  客户端 -> SERVER_IP -> VPS sing-box
```

不要把橙云域名填进直连四协议节点的 `server:`。Cloudflare 普通橙云主要代理 HTTP/HTTPS/WebSocket 流量，不会替你代理 Hysteria2 UDP、VLESS Reality、AnyTLS、SS2022 这种直连代理协议；填错后常见表现是 Reality 443 挂、HY2/SS UDP 不通、AnyTLS/SS TCP 被挡。

如果确实要让 Cloudflare 参与“节点连接”，只能使用专门的 WebSocket/Tunnel 形态，也就是本文的 `CF-Vless-WS`：

```text
CF-Vless-WS:
  客户端 -> Cloudflare -> cloudflared -> VPS 127.0.0.1:28080

主线四协议:
  客户端 -> VPS IP / 灰云直连域名 -> sing-box
```

因此同一个域名最多只承担一种角色，推荐拆开：

```text
sub.example.com      可橙云：只给订阅网页 / 看板页用
direct.example.com   灰云：给 Hysteria2 / Reality / AnyTLS / SS2022 的 server: 用
cf.example.com       橙云/Tunnel：只给 CF-Vless-WS 大保底用
```

客户端添加订阅时，名称和 URL 分开填：

```text
订阅名称: AIRPORT_NAME
订阅 URL: http://SUB_DOMAIN_1SUB_PATH
```

示例：

```text
订阅名称: LINE-01
订阅 URL: http://node.example.com/sub-a8f3c91d7e4b4c6f.yaml
```

如果客户端没有单独的订阅名称输入框，就在添加后把配置名称改成 `AIRPORT_NAME`。

不要把订阅名称写成 URL，也不要写成节点协议名。协议名已经用于节点：

```text
Hysteria2
AnyTLS
Vless
SS2022
```

默认不要用 HTTPS 订阅：

```text
443/tcp = VLESS Reality
80/tcp  = Nginx 订阅
```

443 已经留给 VLESS Reality，VPS 本机不再单开 443 做 HTTPS 订阅。如果确实想要加密订阅，两条路子二选一（都在「订阅传输安全」一节）：① 用单独的订阅域名 `CF_SUB_HOSTNAME`（或对 `CF_HOSTNAME` 按 path 分流）经 Cloudflare Tunnel 回源到本机 Nginx(80)；② 不依赖 Cloudflare 时，按「直连 HTTPS: Nginx 443 SNI 分流」用 stream 前端在 443 上同时承载 Reality 和 HTTPS 订阅。注意订阅不能直接打 `CF_HOSTNAME`——那个 Host 回源的是 VLESS WS 入站。

## IPv4 / IPv6 / 双栈

前面的主线模板默认按 **IPv4** 写（节点 `server:` 用 `SERVER_ADDRESS`，IPv4 默认让 `SERVER_ADDRESS=SERVER_IP`；DNS 配 `A`，`ipv6: false`，Nginx `listen 80` + `[::]:80`，分流用 `IP-CIDR,.../32`）。绝大多数 VPS 这样就够了。只有当机器是纯 IPv6 或双栈、且你确实要利用 IPv6 时，才参考下面按情况调整。

为不打乱主线，这里**不重命名** `SERVER_IP`（它仍是 IPv4 字面量，只给 IPv4/双栈分流规则用）；需要 IPv6 时按下面补充变量即可：

```text
SERVER_IP        主线 IPv4 地址（IPv4/双栈机器有，纯 IPv6 机器没有）；
                 IPv4/双栈 rules 的 IP-CIDR/32 用它，纯 IPv6 改用 IP-CIDR6/128
SERVER_IPV6      机器的公网 IPv6，仅 IPv6/双栈用到；rules 的 IP-CIDR6/128 用它
SERVER_ADDRESS   节点连接地址（主模板 server: 已改用它）。IPv4 默认填 SERVER_IP；
                 IPv6-only 填域名 SUB_DOMAIN_1，避免写 IPv6 字面量
```

一句话总览：**IPv4-only 最简单；双栈要防客户端乱选 IPv6；IPv6-only 最好用域名，并准备 Cloudflare Tunnel 保底。**

### IPv4-only（最常见，但先确认内核 IPv6 是否启用）

```text
变量：SERVER_ADDRESS = SERVER_IP（IPv4 字面量）。
DNS：只配 A 记录，不配 AAAA。
Mihomo 模板：ipv6: false 可以保留。
Clash 订阅：server: SERVER_ADDRESS 解析成 IPv4。
分流规则：保留 - IP-CIDR,SERVER_IP/32,DIRECT,no-resolve。

sing-box listen 和 Nginx，看内核 IPv6 栈状态分两种：
  ① IPv6 栈没禁用（默认情况）：listen 用 "::"、Nginx 保留 [::]:80 都没问题，不用改。
  ② IPv6 栈被显式禁用（sysctl disable_ipv6=1）：sing-box listen 必须改 "0.0.0.0"，
     Nginx 必须删掉 listen [::]:80 只留 listen 80，否则服务起不来 / 绑定失败。
```

### IPv6-only

```text
变量：SERVER_ADDRESS = SUB_DOMAIN_1（用域名，别写 IPv6 字面量）。
DNS：只配 AAAA，不配 A。
前提：客户端网络必须有 IPv6，否则手机/电脑根本连不到这台 VPS（这是最大门槛）。
Mihomo 模板：必须改 ipv6: true，否则 AAAA 解析和 IPv6 连接被禁用，照抄会连不上。
sing-box：listen 用 "::"。
Nginx：保留 listen [::]:80；有 IPv4 栈时 listen 80 留着也无妨。
Clash 订阅：server: SERVER_ADDRESS 已是域名 SUB_DOMAIN_1。
分流规则：用 - IP-CIDR6,SERVER_IPV6/128,DIRECT,no-resolve（原来的 /32 只适合 IPv4）。
订阅 URL：用域名 http://node.example.com/sub-xxx.yaml，不要写 http://[IPv6]/...（兼容性差）。
```

### 双栈 IPv4 + IPv6

```text
变量：SERVER_ADDRESS = SERVER_IP（默认走 IPv4）；要走 IPv6 则填对应域名。
Mihomo 模板：想让节点走 IPv6 时改 ipv6: true；只把 IPv6 当备用测试可保留 false。
DNS：可同时配 A 和 AAAA。
风险：客户端可能优先走 IPv6，若其 IPv6 链路不稳，会出现"有时连得上有时连不上"。
稳妥做法：拆两个域名分别只配一种记录，例如
         node4.example.com 只配 A（SERVER_ADDRESS=node4...），
         node6.example.com 只配 AAAA（SERVER_ADDRESS=node6...）。
         共用一个域名时，可参考 mihomo 代理通用字段的 ip-version
         强制选栈，常用值如 ipv4 / ipv6 / ipv4-prefer / ipv6-prefer。
省事做法：主线先只用 IPv4，IPv6 当备用测试。
分流规则：两条都加
         - IP-CIDR,SERVER_IP/32,DIRECT,no-resolve
         - IP-CIDR6,SERVER_IPV6/128,DIRECT,no-resolve
```

> 开启 `ipv6: true` 后补本地 IPv6 直连规则：主模板的私网/本地直连段只有 IPv4（`127.0.0.0/8`、`10/8`、`192.168/16` 等）。一旦走 IPv6，本地 IPv6 流量会因为没有匹配规则而误走代理。在 `rules` 的私网段附近补上：
>
> ```yaml
>   - IP-CIDR6,::1/128,DIRECT,no-resolve        # IPv6 回环
>   - IP-CIDR6,fc00::/7,DIRECT,no-resolve        # 唯一本地地址 ULA
>   - IP-CIDR6,fe80::/10,DIRECT,no-resolve       # 链路本地
>   - IP-CIDR6,ff00::/8,DIRECT,no-resolve        # 组播
> ```

### 纯 IPv6 机器的保底

纯 IPv6 VPS 直连做节点有门槛——很多客户端网络仍是 IPv4-only，连不到 AAAA。这种机器更适合把 Cloudflare Tunnel / CF-Vless-WS 当**重要保底**（不只是大保底）：

```text
客户端连 Cloudflare 域名，Cloudflare 边缘同时支持 IPv4/IPv6。
VPS 上的 cloudflared 主动向 Cloudflare 出站连接（不需要 VPS 有 IPv4 入站）。
这样 IPv4-only 客户端也能通过 Cloudflare 访问 IPv6-only VPS 后面的 WS 节点。
```

但要清楚边界：HY2 / AnyTLS / Reality 这类**直连**协议不能靠 Cloudflare 普通 DNS 代理救活（Cloudflare 7 层代理只认 HTTP(S)/WS）。纯 IPv6 机器要给 IPv4-only 客户端用，就得走 WS/Tunnel 形态，也就是这里的 CF-Vless-WS。

## 检查命令

服务状态：

```bash
systemctl is-active sing-box nginx vnstat cron
sing-box check -c /etc/sing-box/config.json
nginx -t
timedatectl    # 确认 NTP 同步，时钟正常
```

系统是否被 I/O / 内存拖卡（“服务 active 但节点像挂了”时优先看这里）：

```bash
uptime
free -m
vmstat 1 5
cat /proc/pressure/io /proc/pressure/memory /proc/pressure/cpu
ps -eo pid,ppid,stat,wchan:24,comm,args | awk 'NR==1 || $3 ~ /^D/'
journalctl --disk-usage
```

判断口径：

```text
CPU idle 很高但 wa(I/O wait) 很高：不是 CPU 不够，是磁盘 / 日志 / 内核等待把系统拖住。
io pressure full 长时间很高：SSH、cloudflared、sing-box 都会像“卡死”。
出现 D 状态的 systemd-journal / cloudflared / sing-box：优先降日志、停非必要常驻服务，必要时重启。
```

端口（注意：只证明本地在监听，证明不了云安全组放行；外部可达要从别的机器测）：

```bash
ss -lntup | grep -E ':(80|443|4433|4434|4435)\b'
```

订阅路径有没有替换干净（应无输出）：

```bash
grep -n 'SUB_PATH' /etc/nginx/sites-enabled/default
```

服务端核心配置占位符是否替换干净（应无输出）：

```bash
# 扫服务端配置文件里的核心占位符，漏替换的会在哪个文件第几行直接显示。
# 注意：AIRPORT_NAME 是客户端订阅名、不落在这些文件里，所以不在此扫描范围，部署时单独核对。
grep -rnE 'SERVER_(IPV6|IP|ADDRESS)|SUB_DOMAIN_[12]|SUB_PATH|SUB_B64_PATH|HY2_PASSWORD|ANYTLS_PASSWORD|VLESS_UUID|REALITY_(PRIVATE|PUBLIC)_KEY|REALITY_SHORT_ID|REALITY_SNI|SS_PASSWORD|PANEL_(LOGIN_)?PATH|CF_(HOSTNAME|SUB_HOSTNAME|WS_PATH|VLESS_UUID)|TUNNEL_(ID|NAME)' \
  /etc/sing-box/config.json \
  /etc/nginx/sites-enabled/default \
  /var/www/htmlSUB_PATH \
  /etc/cloudflared/config.yml \
  /etc/nginx/conf.d/90-subscription-https.conf \
  /etc/nginx/stream.d/90-subscription-https.conf 2>/dev/null
# 后两个文件只有做了「直连 HTTPS: Nginx 443 SNI 分流」的机器才有；没做就自动跳过。
```

订阅：

```bash
curl -I http://SUB_DOMAIN_1SUB_PATH
curl -I http://SUB_DOMAIN_1/sub.yaml
```

CF 大保底单独检查（只在部署了 `CF_HOSTNAME` 时执行）：

```bash
# 本机入站：能 101 说明 sing-box 的 CF-Vless 本地 WS 入站没坏。
curl -i --http1.1 \
  -H "Host: CF_HOSTNAME" \
  -H "Connection: Upgrade" \
  -H "Upgrade: websocket" \
  -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
  -H "Sec-WebSocket-Version: 13" \
  http://127.0.0.1:28080CF_WS_PATH

# 公网入口：能 101 才说明 DNS + Cloudflare Tunnel + 回源都正常。
curl -i --http1.1 \
  -H "Connection: Upgrade" \
  -H "Upgrade: websocket" \
  -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" \
  -H "Sec-WebSocket-Version: 13" \
  https://CF_HOSTNAMECF_WS_PATH

systemctl is-active cloudflared
journalctl -u cloudflared -n 80 --no-pager | grep -Ei 'Registered|timeout|failed|error|quic|http2'
```

```text
订阅 200，但 CF_HOSTNAME 返回 502/530/1033：订阅没坏，是 Cloudflare Tunnel / cloudflared 大保底坏。
本机 127.0.0.1:28080 能 101、公网不能 101：sing-box 没坏，查 cloudflared / DNS / Tunnel。
本机也不能 101：查 sing-box 的 cf-vless-ws-in 入站和 CF_WS_PATH / CF_VLESS_UUID。
```

限流脚本日志：

```bash
journalctl -t traffic_limit -n 20 --no-pager
```

预期：

```text
随机订阅路径: 200 OK，有 Subscription-Userinfo
/sub.yaml: 404，无 Subscription-Userinfo
Server: nginx，不暴露版本号
grep SUB_PATH: 无输出（说明已替换成真实路径）
服务端核心占位符扫描: 无输出（说明 IP/域名/密码/UUID/CF 变量都替换干净了）
```

## 修改前备份

```bash
ts=$(date +%Y%m%d-%H%M%S)
mkdir -p /root/vps-config-backups/$ts
chmod 700 /root/vps-config-backups
cp -a /etc/sing-box/config.json /root/vps-config-backups/$ts/
cp -a /etc/sing-box-node.env /root/vps-config-backups/$ts/
cp -a /var/www/htmlSUB_PATH /root/vps-config-backups/$ts/
cp -a /etc/nginx/sites-enabled/default /root/vps-config-backups/$ts/
cp -a /etc/nginx/snippets/sub_headers.conf /root/vps-config-backups/$ts/
cp -a /usr/local/bin/traffic_limit.py /root/vps-config-backups/$ts/
cp -a /etc/cron.d/traffic_limit /root/vps-config-backups/$ts/

# 如果部署了 CF 保底，连同隧道凭证一起备份（凭证敏感，别外传）
[ -d /etc/cloudflared ] && cp -a /etc/cloudflared /root/vps-config-backups/$ts/
```

## 之前改动抽象成通用规则

这些不是某一台 VPS 专属，而是以后都建议这样做：

| 项目 | 通用最终做法 | 原因 |
|---|---|---|
| 文档 | 只保留一个 `节点搭建文档.md` | 避免多个版本混乱 |
| 订阅名称 | 使用 `AIRPORT_NAME` | 多条线路时一眼看出是哪条线路 |
| 订阅路径 | 使用随机路径 | `/sub.yaml` 太容易被扫到 |
| 旧路径 | 返回 404 | 不继续暴露旧订阅 |
| 节点名 | `Hysteria2` / `AnyTLS` / `Vless` / `SS2022` | 简洁，不暴露实现细节 |
| 代理组 | `type: select` | 手动切换最方便排查 |
| sing-box 日志 | 服务端默认 `warn`，排查时才临时 `info` | 低配 VPS 上大量连接日志会拖高 journald I/O wait |
| 运行参数 | 统一进 `/etc/sing-box-node.env` | 单一来源，避免改了一处忘另一处 |
| 流量计费 | 默认 `COUNT_MODE=tx` | 多数商家只按出站计费；代理 rx≈tx，rx+tx 会把用量翻倍误算、提前半量停机 |
| nginx reload | 头变化才 reload | 流量持续增长时每轮 reload 属正常；无统计变化才跳过 |
| 限流停机 | 标记文件区分“限额停”和“手动停” | 手动停机不被定时任务拉起 |
| 限流报错 | stderr 接 `logger` | 网卡写错等故障可见，不静默失效 |
| 防火墙 | ufw + 云安全组放行当前实际监听端口 | 普通 VPS 要注意 UDP；阿里云清洗敏感机不要把不用的 UDP 端口放回去 |
| 时间同步 | `timedatectl set-ntp true` | Reality/TLS 握手对时钟敏感 |
| Google Play | 高优先级走代理 | Play 下载链路域名多，必须放在 `GEOSITE,cn` / `GEOIP,CN` 前，避免下载 CDN 被误判直连导致卡住 |
| 国内分流 | 常用域名 + `GEOSITE,cn` + `GEOIP,CN` | 减少国内 App 误走代理 |
| 流量头 | 放到 `snippets`，只在订阅路径 include | 避免 404 和首页也带流量头 |
| Nginx 首页 | `return 404` | 不暴露默认欢迎页 |
| Nginx 版本 | `server_tokens off` | 少暴露版本 |
| sing-box 权限 | `600 root root` | 配置里有密码和私钥 |
| 订阅传输 | 默认 HTTP，需加密走 CF Tunnel | 443 留给 Reality；明文订阅靠随机路径降低风险 |
| HTTPS 订阅 | 默认不启用 | 443 留给 VLESS Reality |
| Cloudflare | 默认不用 | 直连四协议更简单、好排查 |
| 大保底 | `CF-Vless-WS`，并且用独立 `CF_HOSTNAME` | 只在 IP 直连出问题时使用；Tunnel 挂不应影响订阅域名 |
| cloudflared | 低配 / 丢包机器优先 `protocol: http2` + `loglevel: warn` | QUIC 抖动时会出现 Tunnel down、502、1033 |
| 阿里云清洗 | HY2/UDP 触发过清洗的机器，稳定优先就移除 HY2；速度优先可恢复 HY2 但必须限速 | 系统默认阈值已最高时，继续压测 UDP 仍可能被平台清洗 |
| 协议组合 | 稳定优先：`AnyTLS + Vless + SS2022(TCP-only) + CF-Vless`；速度优先：恢复限速 HY2 | 阿里云小鸡不能把 HY2 当满血不限速下载机器 |
| 备份 | 含 env / cron / cloudflared 凭证 | 否则迁移后限流失效、隧道无法恢复 |

## 一键脚本和面板的取舍

默认主线仍然是本文档：`sing-box + nginx + vnstat/cron`，不依赖网页面板。长期自用时，优先按本文档手搓，或者使用自己的 `sing-box-oneclick` 做同一套架构的自动化部署。

第三方一键脚本（例如 3x-ui、其它 sing-box / Xray 全家桶）可以拿来参考或临时测试，但不要作为本文档默认主线。它们功能多、更新快，也可能带来额外服务、额外端口、额外配置逻辑；出问题时不如本文档这套文件路径和排障口径清楚。

低配 VPS / AWS 免费机 / 只给自己用的机器，尤其不建议默认装面板：

```text
推荐：sing-box + nginx + vnstat/cron
按需：CF-Vless-WS、WARP、doctor、backup/restore
不默认：3x-ui / s-ui / 宝塔 / 其它公网管理面板
```

如果只是想快速复刻本文档架构，用自己的 `sing-box-oneclick` 更合适；如果要多用户售卖、网页上分配账号、统计每个用户流量，才考虑专门面板。面板即使只监听 `127.0.0.1`，也属于额外常驻组件；能不用就不用。

## 可选增强（一键脚本已实现，手搓按需加）

下面这些都不是四协议主线的必需项，而是**可选件**。我的一键脚本（`install.sh`）里已经把它们全做好了，并用真 sing-box 二进制 `sing-box check` 验证过配置合法。手动部署时，按需挑着加即可；不加也不影响主线。

> **脚本来源 / 本文档的边界**：表里凡是写“脚本 `install.sh xxx`”的（看板页 `panel`、网页管理 `admin`、`warp`、`doctor`、`harden`、`backup/restore`），完整实现都在仓库 **`github.com/shiro1888/sing-box-oneclick`**（`install.sh`）。**光靠这份 md 手搓不出这些复杂可选件**——本文档只保证“主线四协议 + 订阅 + 流量显示/限流”能纯手工复现；上面这些自动化增强请去仓库拿脚本一键装。简单的（禁 BT、obfs、sysctl 调优）md 里给了可手搓的要点。

| 增强 | 作用 | 怎么加 |
|---|---|---|
| **服务端路由**：禁 BT/PT + 拦广告 | 在 sing-box 加 `route` 段：`bittorrent` 直接 reject（防被商家收滥用投诉封机），geosite 广告域 reject | 手搓在 `config.json` 加 `route`；或用脚本默认就开 |
| **HY2 obfs（salamander）** | 给 HY2 的 QUIC 包再裹一层混淆，抗“UDP/QUIC 指纹识别” | 服务端 HY2 入站加 `obfs`，客户端同步加 `obfs/obfs-password`；阿里云清洗敏感机器慎用 |
| **HY2 服务端带宽护栏** | 按套餐峰值给 HY2 留余量，防压力测试 / 多人下载把 UDP 队列和 I/O wait 打爆 | 200 Mbps 峰值机器先用 `up_mbps=80`、`down_mbps=160`；如果仍触发清洗，直接移除 HY2 |
| **HY2 端口跳跃** | nftables 把一整段 UDP 端口重定向到真实 HY2 端口，抗运营商按端口限速（**云安全组要放整段 UDP**） | 手搓写 nftables NAT 规则 + systemd；阿里云小鸡不建议默认开，端口段 UDP 更像异常流量 |
| **HY2 brutal** | 客户端订阅写死上下行带宽，让 HY2 用暴力拥塞控制顶满带宽 | 客户端 HY2 节点加 `up/down`；已触发 DDoS 清洗的机器不要再用它压测 |
| **WARP 解锁分流** | 机房 IP 被 ChatGPT/流媒体拉黑时，把这些站点单独走 Cloudflare WARP 出口，其余直连 | 比较复杂（wgcf 注册 + sing-box 加 wireguard 出站 + route 分流），**建议直接用脚本 `install.sh warp`** |
| **备份 / 迁移** | 把密钥+参数+证书打包，换机一条命令重建、凭证不变 | 见本文「修改前备份」；脚本是 `backup` / `restore` |
| **SSH 加固** | 仅密钥登录 + 禁密码 + fail2ban（**先确认密钥能登录再禁密码**，别锁死自己） | 手动改 `sshd_config`；或脚本 `install.sh harden`（带防锁死护栏） |
| **doctor 自检** | 一次性核对服务/配置/sysctl/防火墙/时间/证书/订阅可达 | 见本文「检查命令」；脚本是 `install.sh doctor` |
| **可视化看板页** | 一个只读 HTML：看订阅/扫码/复制/每节点二维码/流量到期；页面含凭证，随机路径不是登录保护 | 脚本 `install.sh panel`；装完建议立刻 `install.sh panel-pass "$(openssl rand -hex 12)"` 加登录（纯静态，不开管理端口） |
| **网页管理面板** | 网页上改限额/到期/重启/备份。**只监听 `127.0.0.1` + token + 走 SSH 隧道**，不暴露公网 | 脚本 `install.sh admin`（别把端口改成 `0.0.0.0`） |
| **网络调优 sysctl + BBR** | `rmem_max/wmem_max=16MB`（HY2/QUIC 跑满带宽的前提）+ BBR v1；性能优先按 byJoey BBRv3 Max + 菜单 12，保守机器可停在 BBR v1 / XanMod / 标准版 | 见下方「网络加速」一节（给了可直接跑的命令） |

> 这些增强里，**最值得手动也加一下的是网络调优**（16MB UDP 缓冲对 HY2 吞吐影响很大）和**服务端禁 BT**（防封机）。WARP / 端口跳跃 / 网页管理面板这类复杂的，建议直接用一键脚本，别手搓踩坑。

## 网络加速：sysctl 调优 + BBR v3 性能优先方案

上面「可选增强」提到的网络调优，这里给可直接执行的版本。**强烈建议至少做第一步 sysctl**（免重启、零风险，对 HY2 吞吐提升明显）。本文档的性能优先路线是：确认机器是 KVM / 云服务器 / 独立服务器，并且有控制台/VNC/救援模式后，使用 byJoey BBRv3 选择 **Max**，再开启菜单 **12「疯批模式」**；保守生产机器可停在 BBR v1，或改用 XanMod / 标准版。

### 第一步：sysctl 调优（免重启，强烈建议做）

```bash
cat >/etc/sysctl.d/99-singbox.conf <<'EOF'
# UDP/QUIC 大接收缓冲 —— Hysteria2 关键！不设会被限速并刷 quic-go 缓冲告警（官方推荐 16MB）
net.core.rmem_max=16777216
net.core.wmem_max=16777216
# TCP 缓冲，适配跨境高带宽-延迟积（BDP）链路
net.ipv4.tcp_rmem=4096 87380 16777216
net.ipv4.tcp_wmem=4096 65536 16777216
net.core.netdev_max_backlog=10000
net.core.somaxconn=4096
net.ipv4.tcp_mtu_probing=1
net.ipv4.tcp_slow_start_after_idle=0
net.ipv4.tcp_fastopen=3
# BBR v1（纯 sysctl、免重启、零风险）
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
EOF

sysctl --system
# 验证
sysctl net.core.rmem_max                 # 期望 16777216
sysctl net.ipv4.tcp_congestion_control   # 期望 bbr
```

> 容器型机器（OpenVZ/LXC）共享宿主内核，上面部分 sysctl 可能被限制改不动——失败不影响代理，能改多少算多少。KVM 全部生效。

### 第二步（进阶）：BBR v3 —— 性能优先，但先确认救援能力

第一步已经开了 **BBR v1**。按本文档的性能优先策略，满足条件后继续上 **byJoey BBRv3 Max + 菜单 12**；但它需要**换内核**，所以单独列出，先确认风险边界：

```text
为什么仍要加护栏：
1. BBR 是内核 TCP 拥塞控制，帮的是 AnyTLS / Vless 这类 TCP 节点；
   Hysteria2 走 UDP/QUIC、自带拥塞控制，完全不受 BBR 版本影响。
   本套是 HY2 优先，v3/Max/菜单 12 主要是给 TCP 节点和极限测速加上限。
2. 换内核比 sysctl 风险大：要重启，万一新内核起不来要能进救援。
3. 只有 KVM 能换内核；OpenVZ/LXC 共享宿主内核，换不了。

动手前提：这台 VPS 必须有控制台/VNC 或救援模式（内核起不来能进去救），一台一台来，别批量。
```

### CloudPassenger/Cloud-Kernel-BBRv3（可选；适合想在 arm64 上试 BBR3）

NodeSeek 帖子「记录启用 bbr3 的步骤」提到的方案是：

```text
项目：CloudPassenger/Cloud-Kernel-BBRv3
地址：https://github.com/CloudPassenger/Cloud-Kernel-BBRv3
内核：Debian Cloud 6.12 系定制内核
支持：amd64 / arm64(aarch64)
包含：BBRv3、BBRPlus、BBRv1(bbr1)、TCP Brutal
```

这个项目比很多只面向 x86_64 的 BBR3 脚本更适合甲骨文 ARM：它的 Releases 里确实有 `*-arm64` 包，例如 `6.12.94-arm64`。如果你是 Oracle ARM A1.Flex、Ubuntu/Debian、想试 BBR3，可以把它列为候选。

但本文档对甲骨文机器的默认建议仍然是：

```text
先用发行版官方内核 + fq + bbr。
CloudPassenger BBR3 可以试，但不是必需。
不要为了轻量节点机上来就换内核。
不要用 BBR3 当“突破带宽上限”的工具；它只影响 TCP 拥塞控制，不会突破 Oracle 的网络上限。
```

更具体地说：

```text
普通 BBR：建议先开，免重启、风险低、够大多数节点机使用。
CloudPassenger BBR3：适合测试机、可救援机器、或明确想优化 TCP 晚高峰抖动的机器。
甲骨文 ARM 主力机：先别急；除非你已经确认控制台/救援模式可用，并且愿意承担换内核风险。
```

BBR3 能影响的主要是 TCP：

```text
可能有收益：AnyTLS、VLESS Reality TCP、SS2022 TCP、SSH、HTTP/HTTPS。
基本无收益：HY2、TUIC、QUIC、UDP。
不会改变：Oracle 官方带宽上限、本地宽带瓶颈、晚高峰国际出口拥堵的硬上限。
```

如果要试 CloudPassenger，先做检查：

```bash
uname -m
uname -r
sysctl net.ipv4.tcp_available_congestion_control
```

确认架构：

```text
aarch64 / arm64 机器：只能装 arm64 包
x86_64 / amd64 机器：只能装 amd64 包
```

帖子里的安装入口是：

```bash
wget https://raw.githubusercontent.com/CloudPassenger/Cloud-Kernel-BBRv3/main/install-kernel.sh
chmod +x install-kernel.sh
./install-kernel.sh
```

交互里选语言后，一般选 `0` 最新版即可。安装重启后验证：

```bash
uname -r
sysctl net.ipv4.tcp_congestion_control
modinfo tcp_bbr
```

期望：

```text
uname -r 显示新装的 6.12.xx 内核
sysctl net.ipv4.tcp_congestion_control = bbr
modinfo tcp_bbr 能看到 version 3 / 或该内核说明 bbr 已是 BBRv3
```

CloudPassenger BBR3 的 sysctl 可以沿用本文第一步的 `fq + bbr + mtu_probing`，小内存机不要把缓冲区拉太夸张。1–2GB 内存机器建议保持 16MB：

```bash
cat >/etc/sysctl.d/99-bbr3.conf <<'EOF'
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
net.ipv4.tcp_ecn=1
net.ipv4.tcp_mtu_probing=1
net.ipv4.tcp_rmem=4096 87380 16777216
net.ipv4.tcp_wmem=4096 65536 16777216
net.core.rmem_max=16777216
net.core.wmem_max=16777216
net.core.netdev_max_backlog=8192
net.core.somaxconn=4096
net.ipv4.tcp_slow_start_after_idle=0
net.ipv4.tcp_fastopen=3
EOF

sysctl --system
```

> 注意：网上帖子里常见 `cat >> /etc/sysctl.d/99-bbr3.conf` 是追加写入，多跑几次会把同一组配置重复写进去。本文统一用 `cat > ...` 覆盖写入；如果机器上已有重要自定义 sysctl，先 `cp /etc/sysctl.d/99-bbr3.conf /root/99-bbr3.conf.bak` 再改。

GRUB 注意事项：

```text
如果装完重启后仍进官方内核，可能是 GRUB 认为带 +deb 后缀的官方内核版本更高。
先用 awk 提取菜单名，再改 GRUB_DEFAULT 指向 BBR3 内核。
不要凭感觉手写菜单名；必须和 grub.cfg 里的一字不差。
```

查看菜单：

```bash
awk -F\' '/menuentry / {print $2}' /boot/grub/grub.cfg
```

示例：

```bash
nano /etc/default/grub
# 例：按你机器实际输出修改，下面只是示例
GRUB_DEFAULT="Advanced options for Debian GNU/Linux>Debian GNU/Linux, with Linux 6.12.94"

update-grub
reboot
```

重要取舍：

```text
不要急着 apt autoremove --purge 删除官方原内核。
至少确认新内核能正常重启、SSH 正常、sing-box 正常、网络正常，再考虑清理。
甲骨文 ARM 主力机建议长期保留一个官方内核作为回滚入口。
```

不建议照抄“卸载官方内核保持干净”这一步：

```text
测试机：确认无误后可以清理旧内核。
主力机：保留官方内核更重要，别为了“干净”牺牲回滚能力。
Oracle ARM：尤其建议保留官方原内核；新内核起不来时，它就是救命后路。
```

保守 x86_64 机器可用 **XanMod 内核**（自带 BBRv3，有官方 apt 源 + 签名，比装某个 GitHub 预编译内核更可信、好维护）。只有明确追求极限测速、且有救援通道的机器，才跳到下面 byJoey 段走 Max + 菜单 12：

> 适用范围：下面这段**只给 x86_64 / KVM 机器**用，装的也是 x86-64 专用包（`linux-xanmod-x64v3`）。
> arm64 机器不要照抄——XanMod 这个源没有对应包；arm64 想要 BBRv3 可以看上面的 CloudPassenger/Cloud-Kernel-BBRv3，否则保持发行版内核的 `fq + bbr`（第一步 sysctl 已配）即可，多数场景够用。

```bash
# 0) 看 CPU 指令集等级（决定装哪个包：v1/v2/v3/v4）
# 落盘执行, 不用 wget -qO- | bash: -q 会把 403/网络错误吞成空输出、盲跑上游脚本也不安全。
# XanMod 站点对命令行 UA 可能返回 403, 失败就加 -U "Mozilla/5.0" 或换浏览器打开确认。
wget -U "Mozilla/5.0" -O psabi.sh https://dl.xanmod.org/check_x86-64_psabi.sh
less psabi.sh && bash psabi.sh
# 若上面 apt 源 / archive.key 取不到（同样可能 403 或源已调整），以 xanmod.org 官网当前指引为准。

# 1) 加 XanMod 官方源（gpg --dearmor 需要 gnupg；基础依赖没装，这里先补上。
#    keyrings 目录极简镜像可能也没有，一并建好）
apt install -y gnupg
mkdir -p --mode=0755 /usr/share/keyrings
wget -qO - https://dl.xanmod.org/archive.key | gpg --dearmor -o /usr/share/keyrings/xanmod-archive-keyring.gpg
echo 'deb [signed-by=/usr/share/keyrings/xanmod-archive-keyring.gpg] http://deb.xanmod.org releases main' \
  > /etc/apt/sources.list.d/xanmod-release.list

# 2) 装内核（x64v3 是 CPU 指令集等级、不是 BBR 版本；按第 0 步结果选 v1/v2/v3）
apt update && apt install -y linux-xanmod-x64v3

# 3) 重启进新内核
reboot

# 4) 回来验证
uname -r                                  # 内核名里应带 xanmod
sysctl net.ipv4.tcp_congestion_control    # 期望 bbr（XanMod 里这个 bbr 就是 BBRv3）
```

> 第一步写的 `/etc/sysctl.d/99-singbox.conf` 里已经是 `fq + bbr`，换上 XanMod 内核后**自动用上 v3**，不用再改 sysctl。
>
### byJoey BBRv3 内核（可选；具体内核版本随上游更新，部署时现场查询）

[byJoey/Actions-bbr-v3](https://github.com/byJoey/Actions-bbr-v3) 现在已经不是单纯放 `.deb` 的仓库，而是一个 **BBRv3 管理脚本**：自动识别 `x86_64` / `aarch64`，从 GitHub Releases 选匹配架构的内核，支持安装/更新、指定版本、状态检查、队列算法切换、亚太 TCP 调优、智能带宽优化和卸载。

> 下面的菜单项、`Max` / 标准版命名、`*-max` 资产名等都是**写文档时该脚本的行为**；它是第三方项目、随时可能改。实际**以你运行时 byJoey 仓库的菜单和 Releases 命名为准**，下述名称仅供参考。

适用前提仍然不变：

```text
Debian 12+ / Ubuntu 24.04+
apt-get
x86_64 或 aarch64
KVM / 云服务器 / 独立服务器
建议 GRUB 引导
必须有控制台、VNC 或救援模式
```

入口命令（换内核是全文最高风险操作，别直接管道执行第三方脚本——先落盘看一眼再跑）：

```bash
# 第三方脚本, 上游随时可能改。落盘 → 浏览确认 → 再执行, 不要 bash <(curl ...) 盲跑。
curl -fsSL https://raw.githubusercontent.com/byJoey/Actions-bbr-v3/main/install.sh -o bbr3.sh
less bbr3.sh          # 看一眼再决定跑不跑
bash bbr3.sh
```

常用菜单：

```text
1  安装或更新 BBR v3 最新版
2  指定版本安装
3  检查 BBR v3 状态
4  启用 BBR + FQ
5  启用 BBR + FQ_CODEL
6  启用 BBR + FQ_PIE
7  启用 BBR + CAKE
8  亚太机器 TCP 调优
10 BBR v3 智能带宽优化
11 清空网络优化配置
12 BBR v3 疯批模式（极限测速挑战）
```

> byJoey 内核按机型分档选，不要无脑上 Max（这和全文「稳定优先」原则一致）：
>
> ```text
> 低配(≤1GB) / 清洗敏感 / 生产自用机 → 标准版, 不开菜单 12。稳定压倒峰值。
> 仅测速导向, 且有控制台/VNC/救援通道 → 性能优先分支: Max 版 + 菜单 12「疯批模式」。
> ```
>
> 菜单 **「1 安装最新版」** 会让你选标准版或 Max；按上表选。实测佐证见文末「实例记录」——那台阿里云 0.5GB 小机就是从 `7.1.0-joeyblog-bbrv3-max` 回退到 XanMod，稳定优先不用 Max 冲测速。
>
> 注意：菜单 **12「疯批模式」** 是另一层额外激进的网络调参/测速模式，不等于 Max 内核本身。按本文档的性能优先策略，装好 Max 后可以继续开启 12；如果开启后出现丢包、网页卡顿、SSH 抖动或视频加载忽快忽慢，用菜单 **11「清空网络优化配置」** 回滚这层调参，再保留 Max 内核观察。

Max + 菜单 12 开完后，不要只看测速峰值，先做 15-30 分钟健康观察：

```bash
uname -r
uptime
vmstat 1 5
cat /proc/pressure/io /proc/pressure/memory /proc/pressure/cpu
systemctl is-active sing-box cloudflared nginx
journalctl -u cloudflared -n 80 --no-pager | grep -Ei 'timeout|failed|error|quic|Registered'
```

```text
如果 load 长时间很高、vmstat 的 wa 经常飙高、io pressure full 很高，
或 cloudflared 反复 no recent network activity / handshake timeout：
先把 sing-box / cloudflared 日志降到 warn，停掉不必要常驻服务；
仍不稳再用菜单 11 清空疯批模式调参；
还不稳才考虑从 Max 回退标准版 / XanMod / 发行版内核。
```

选择内核时按机型分档（同上文的机型判定表），不设统一默认：

```text
Max：性能优先分支；提高 Startup / ProbeBW / cwnd 进攻性，追求更高吞吐；仅测速导向 + 有救援通道的机器用
标准版：低配 / 清洗敏感 / 生产自用机的默认；更保守；Max 出现抖动、丢包敏感或重启后不稳时也回退到它
```

版本策略：

```text
文档不要写死某个 7.x.x 版本号。
这里说的“最新内核”，指 GitHub Releases 里当前最新的、匹配本机架构的 Max 版 BBRv3 内核。
让 byJoey 脚本动态读取 GitHub Releases；或自己解析 Releases 时按下面规则过滤：
  性能优先分支 x86_64 机器：取 x86_64-*-max；aarch64/arm64 机器：取 arm64-*-max；
  低配 / 清洗敏感 / 生产自用机：取不带 -max 的标准版；
  菜单 12「疯批模式」只在性能优先分支开启；它不是内核类型，和 Max 内核分开看。
不要无脑使用 /releases/latest，因为 latest 可能指向另一个架构，或指向不符合当前策略的标准版 / Max 版。
```

也就是说，文档里的目标不是“安装某个固定版本”，而是“安装 byJoey GitHub Releases 当前最新的匹配版本”。具体版本号会随仓库更新而变化，部署时现场查询。

> 对已经装了 XanMod 且 `modinfo tcp_bbr` 显示 `version: 3` 的 x86_64 机器，不需要只因为 byJoey 有更新就焦虑换内核。若按本文档的性能优先路线，确认控制台/VNC/救援模式可用后，再切 byJoey Max + 菜单 12；若这台机器更看重长期稳定，就保留 XanMod 或改用 byJoey 标准版。

## 什么时候再加东西

暂时不建议把 Cloudflare WS / Argo / WARP 当主力。它们适合作为特殊备用，不适合作为这个模板的主线。

暂时不建议默认加 TUIC v5。TUIC 也是 UDP/QUIC 直连协议；阿里云已经因 HY2/UDP 触发过清洗的机器，不建议再加 TUIC 做主力或压测。只有在非清洗敏感线路、且明确需要 UDP 直连备用时，再单独评估。

暂时不建议默认加自动测速组。手动 `select` 更适合自用排查。

## 实例记录

这一节是可选填写区。文档主体保持通用模板；如果只想保留一个 `.md`，就在这里记录某台 VPS 的实际参数，不再额外新建多个文档。

填写时不要公开 UUID、密码、私钥、Cloudflare token、Tunnel credentials 等敏感值。

```text
AIRPORT_NAME =
SERVER_IP =
SERVER_IPV6 =
SERVER_ADDRESS =
SUB_DOMAIN_1 =
SUB_DOMAIN_2 =
SUB_PATH =
INTERFACE =
LIMIT_GB =
COUNT_MODE =
EXPIRE_AT =
CF_HOSTNAME =
CF_SUB_HOSTNAME =
CF_WS_PATH =
Cloudflare Tunnel =
当前节点名 = Hysteria2 / AnyTLS / Vless / SS2022 / CF-Vless
```

阿里云清洗敏感机器的实例记录可写成：

```text
当前组合 = Hysteria2(限速) / AnyTLS / Vless / SS2022 / CF-Vless
当前内核 = 6.19.11-x64v3-xanmod1（BBRv3，tcp_bbr version: 3）
已回退 = 从 7.1.0-joeyblog-bbrv3-max 切回 XanMod；阿里云 0.5GB 小机稳定优先，不用 Max 冲测速
已恢复 = Hysteria2 4433/udp；SS2022 4435/udp
HY2 护栏 = up_mbps=80，down_mbps=160；这台阿里云小 VPS 不适合跑满带宽
UDP 状态 = 4433/udp、4435/udp 已监听；AnyTLS / Vless / CF-Vless / SS2022 订阅均 udp: true
验收 = ss -lunp 能看到 4433/udp 和 4435/udp；订阅 200；CF-Vless WS 101；直连 TCP 443/4434/4435 可达
```

如果以后有多台 VPS，可以在这一节下面追加多个小段，但仍然保留在这一个 `节点搭建文档.md` 里。

### 阿里云美国小机实测记录（2026-06-17/18）：HY2 压测触发 DDoS 清洗与恢复

实测记录（2026-06-17，阿里云美国 2C / 0.5GB / 20GB ESSD / 200 Mbps 峰值）：

```text
现象：
一次性跑大带宽压力测试后，表现为“全部节点挂了”，不是单条协议失效。
当时 sing-box 仍可能显示 active 或卡在 deactivating，但客户端已经不可用。

实测证据：
HY2 4433/udp Recv-Q 曾堆到 209216。
vmstat 里的 wa 曾到 92%~93%，说明主要是 I/O wait / 小盘写入拖死，不是单纯 CPU 满。
出现过 D 状态进程，systemd / journald / sing-box / cloudflared 都会跟着变卡。
sing-box 日志里还会混入 outbound i/o timeout、IPv6 network is unreachable、扫描流量等噪音，
但根因仍是压力测试把低配机器的网络队列和 I/O 打爆。

结论：
“无限流量”只代表不限月流量，不代表可以长期满速跑 200 Mbps。
这类 0.5GB 小内存 + 小 ESSD + 200 Mbps 峰值机器，更适合做轻量自用节点，
不要把它当多人满速下载 / 长时间压测机器。

当前推荐：
HY2 服务端先设 up_mbps=80、down_mbps=160。
sing-box / cloudflared 日志保持 warn。
journald 做持久限额。
sing-box 加 Restart=always，并用 watchdog 检查 HY2 UDP 队列，异常时自动重启。
```

阿里云 DDoS 清洗实测注意事项（同样来自 2026-06-17 这台机器）：

```text
这台阿里云美国 200 Mbps 峰值 VPS 在跑 HY2/UDP 压力测试后，阿里云短信提示：
公网 IP（已脱敏）正遭受 DDoS 攻击，已自动启动清洗防护。

控制台里“清洗次数 1、清洗 IP 数 1”，清洗阈值已经是系统默认/最高。
这说明问题不是阈值设太低，而是平台识别到了 UDP 大流量 / 高 PPS / 压测特征。

阿里云 DDoS 基础防护是公网入口前面的平台能力，不能靠卸载 VPS 里的安全 Agent 关闭。
云安全中心“安全防护离线”和 DDoS 清洗不是一回事；真正影响节点的是流量安全里的清洗事件。

如果系统默认阈值已经最高，仍然触发清洗：
1. 不要继续拿 HY2/UDP 做大流量压力测试。
2. 优先日常使用 AnyTLS / Vless Reality 这类 TCP 节点。
3. CF-Vless-WS 只做兜底，不追速度。
4. 对阿里云这种小带宽峰值机器，HY2 可以作为可选项；如果已触发清洗，建议直接移除 HY2。
5. 移除 HY2 后，应同时删除服务端 hy2-in、订阅里的 Hysteria2、看板页 HY2 卡片，并让 4433/udp 不再监听。
6. 阿里云安全组里也可以删掉 4433/udp 入站规则；即使不删，只要服务器不监听 4433/udp，也不会再产生 HY2 流量。

移除 HY2 后的推荐组合：
AnyTLS          日常主用
Vless Reality   稳定备用 / 排障
SS2022          兼容备用；阿里云低配机建议 TCP-only
CF-Vless-WS     Cloudflare Tunnel 兜底

移除 HY2 后的验收标准：
sing-box check -c /etc/sing-box/config.json 通过。
ss -lunp 看不到 4433/udp。
订阅 YAML 里没有 Hysteria2 / hysteria2 / 4433。
通用 base64 解码后没有 hysteria2://。
看板页不再展示 Hysteria2 卡片。
AnyTLS / Vless / SS2022 / CF-Vless 仍在订阅里。
公网测试订阅返回 200，CF-Vless WebSocket 返回 101 Switching Protocols。

2026-06-17 二次复测记录：
阿里云控制台仍可能显示清洗“进行中”，但这不等于节点一定不可用；清洗有观察/冷却时间。
当时控制台显示触发值约 217 Mbps / 27.14 Kpps，上一轮记录里峰值约 399 Mbps / 44.66 Kpps，已经很像 UDP 大流量 / 高 PPS 压测特征。
实测处理后：订阅返回 200，CF-Vless WebSocket 返回 101 Switching Protocols，443/4434/4435 TCP 可达，sing-box / nginx / cloudflared / komari-agent 均 active。
最终恢复处理（2026-06-18）：因实际使用感觉 TCP 节点速度不够，已恢复 HY2 4433/udp 和 SS2022 4435/udp；HY2 保留服务端限速护栏 `up_mbps=80`、`down_mbps=160`。
结论：这台阿里云小带宽峰值机器可以跑 HY2，但**不适合跑满带宽**，也不适合多人长期满速下载或 UDP/QUIC 压测。HY2 只当日常主力/加速体验用，一旦再次触发清洗、丢包、全部节点挂，应优先停用 HY2 或降低限速。

当前 UDP 开启状态（速度优先但限速版）：
| 项目 | 当前状态 | 说明 |
|---|---|---|
| Hysteria2 | 已恢复，`4433/udp` | 服务端限速 `up_mbps=80`、`down_mbps=160`；不要压测跑满 |
| AnyTLS | `udp: true` | 入口仍是 4434/tcp；允许通过 TCP 节点承载 UDP/XUDP |
| Vless Reality | `udp: true` | 入口仍是 443/tcp；允许通过 TCP 节点承载 UDP/XUDP |
| CF-Vless-WS | `udp: true` | 入口是 Cloudflare WebSocket/TLS；只做兜底 |
| SS2022 | `udp: true` | 已恢复 4435/tcp + 4435/udp |
| 公网固定 UDP 入站 | 已开放 | `ss -lunp` 应能看到 4433/udp 和 4435/udp |
```

### 阿里云美国小机实例记录：VLESS Reality 反复 Timeout / SS2022 时间戳 / 清理

记录日期：2026-06-24

机器背景：

```text
机器类型 = 阿里云美国轻量/低配小机
配置特征 = 2C / 0.5GB 内存 / 20GB ESSD / 200 Mbps 峰值
当前内核 = 6.19.11-x64v3-xanmod1（BBRv3）
当前节点 = Hysteria2 / AnyTLS / Vless Reality / SS2022 / CF-Vless
当前承载 = Vless Reality 用 Xray；Hysteria2 / AnyTLS / SS2022 / CF-Vless 用 sing-box
当前 Reality SNI = www.bing.com
```

这次现象：

```text
Hysteria2 / AnyTLS / CF-Vless 能返回延迟。
Vless 一会儿可用，一会儿 Timeout。
SS2022 曾单独 Timeout。
服务端端口 443/4433/4434/4435 都能监听，订阅也能 200，不代表协议一定可用。
```

SS2022 的真实原因：

```text
服务端 sing-box 日志出现：
bad timestamp ... diff 42s

VPS chrony 时间正常，Windows 本机时间慢了约 42 秒。
SS2022 2022-blake3-* 会校验时间戳，时间差过大时服务端直接拒绝，客户端只显示 Timeout。

处理：
Windows 同步时间后，SS2022 恢复。
```

排查 SS2022 时不要只测端口：

```powershell
Test-NetConnection -ComputerName VPS_IP -Port 4435
```

上面只能说明 TCP 端口通。还要看服务端日志：

```bash
journalctl -u sing-box --since '15 minutes ago' --no-pager | grep -i 'bad timestamp\|shadowsocks\|ss-in'
```

VLESS Reality 的真实原因：

```text
最开始怀疑 Xray 进程卡住，重启后短暂恢复。
继续排查后发现更关键的问题是 Reality 伪装目标 www.microsoft.com 偶发从 VPS 侧连接超时。
服务端日志出现过类似：
REALITY: failed to dial dest ... i/o timeout

Reality 服务端握手时要去连 dest / handshake.server。
如果这个伪装目标不稳定，客户端会表现为 Vless Timeout、EOF、connection reset 或 REALITY authentication failed。
```

修复思路：

```text
1. 连续测试多个候选伪装目标：
   www.bing.com / www.apple.com / www.cloudflare.com / www.microsoft.com

2. 选择从 VPS 侧连续多次都稳定的域名。
   这台机器最终选 www.bing.com。

3. 服务端和订阅必须一起改：
   Xray realitySettings.dest
   Xray realitySettings.serverNames
   Clash/Mihomo YAML 的 servername
   base64 URI 的 sni / servername

4. 改 Reality 公钥、short-id、SNI 后，客户端必须刷新订阅并重启 Mihomo/Clash 内核。
```

为什么最后没有让 sing-box 承载直连 VLESS：

```text
sing-box 理论上支持 VLESS Reality。
但这次在 sing-box 1.13.13 服务端 + Mihomo 1.19.27 客户端组合下，实测一直失败：

客户端：
REALITY authentication failed

服务端：
TLS handshake: REALITY: processed invalid connection

尝试过：
- short_id 数组形式
- short_id 单字符串
- 空 short_id
- 重新生成 Reality keypair
- 去掉 xtls-rprx-vision flow
- 把 SNI 从 www.microsoft.com 换到 www.bing.com

结论：
这台阿里云小机当前组合下，不强行用 sing-box 承载直连 VLESS。
最终采用 Xray 承载 443/VLESS Reality，sing-box 承载其余协议。
```

当前推荐架构：

```text
443/tcp       Xray      VLESS Reality Vision
4433/udp      sing-box  Hysteria2
4434/tcp      sing-box  AnyTLS
4435/tcp+udp  sing-box  SS2022
127.0.0.1:28080 sing-box CF-Vless WS 回落
80/tcp        nginx     订阅/看板
```

Xray 版本心得：

```text
排障时发现服务器曾跑 Xray 26.6.1，但 2026-06-24 GitHub Releases 最新稳定版是 v26.3.27。
为了减少变量，回到官方 Releases 稳定版 v26.3.27。
不要只看某个包源里的更高版本号；代理核心排障时，优先用官方 Releases 当前稳定版。
```

更贴近真实客户端的验证方式：

```text
1. Test-NetConnection 只证明端口可达。
2. curl 直连 204 只证明 VPS 出口可用。
3. sing-box 临时客户端能测 sing-box 协议，但不一定等价于 Mihomo 客户端。
4. Mihomo 用户最好用 Mihomo 核心原样加载订阅，再用 204 页面验证。
```

本次有效验收：

```text
Mihomo 核心加载当前订阅后测试 Vless：
http://cp.cloudflare.com -> 204

Xray 日志出现：
accepted tcp:cp.cloudflare.com:80 [vless-reality-in >> direct]

这比只看 443 端口 open 更可靠。
```

清理心得：

```text
可以安全清：
apt 缓存 /var/cache/apt
apt lists /var/lib/apt/lists（清完以后装包前先 apt update）
npm 缓存 /root/.npm
明确的 /tmp 测试残留

暂时不要清：
/root/vps-config-backups
/root/.codex
cloudflared / komari / xray / sing-box / nginx / vnstat / chrony

这次 VLESS 能回滚，就是因为 /root/vps-config-backups 留着。
```

给以后自己的结论：

```text
Vless 挂时不要先重装整套节点。
先确认：其它协议是否正常、443 是否监听、Reality SNI 是否从 VPS 可达、订阅是否同步、客户端核心是否刷新。
如果只有 Vless 掉，优先查 Reality 伪装目标和订阅字段。
如果只有 SS2022 掉，优先查客户端时间和服务端 bad timestamp。
sing-box 能跑 VLESS 不等于每台机器每个客户端组合都值得用 sing-box 跑 VLESS；稳定优先时可以让 Xray 专门负责 Reality。
```

### AWS 免费试用机实例记录：VLESS Reality 间歇 Timeout

记录日期：2026-06-23

机器背景：

```text
机器类型 = AWS 免费试用 EC2
公网 IP = 已脱敏（真实值只留在私有记录里）
流量预算 = 100G 量级，节点侧按 85G 做停机护栏
主线节点 = Hysteria2 / AnyTLS / Vless Reality / SS2022
订阅域名 = sub.example.com 只用于订阅分发，不作为 VLESS Reality 的 server
VLESS 直连端口 = 443/tcp
```

现象：

```text
客户端节点测速里，Hysteria2 / AnyTLS / SS2022 都能返回 300ms 左右延迟。
只有 Vless 显示 Timeout。
这不是整台 VPS 掉线，也不是 AWS 安全组全局拦截；更像 VLESS Reality 单协议链路问题。
```

排查顺序：

```bash
systemctl is-active sing-box nginx
ss -lntup | grep -E ':(443|4433|4434|4435)'
sing-box check -c /etc/sing-box/config.json
journalctl -u sing-box --since '15 minutes ago' --no-pager | tail -n 120
```

判断要点：

```text
1. sing-box / nginx 都是 active，443/4433/4434/4435 都在监听时，不要急着重装。
2. Windows 本地 Test-NetConnection 能通 443，只能证明端口可达，不能证明 Reality 参数正确。
3. sing-box 客户端手动 curl 可能因为重试而成功，不等于 Mihomo 客户端测速稳定。
4. 要贴近 Clash/Mihomo 客户端表现，最好用 Mihomo 的 external-controller delay API 复现。
5. 日志里的 ipv6.msftconnecttest.com / ipv6check-http.steamserver.net 失败，在无 IPv6 出口的 AWS 机器上常见；它可能是噪音，不一定是 VLESS Timeout 的根因。
```

本次实锤复现：

```text
用 Mihomo 原样加载订阅后：
Hysteria2 / AnyTLS / SS2022 的 delay API 能稳定返回延迟。
Vless 的 delay API 会间歇返回：
{"message":"An error occurred in the delay test"}

手动网页访问有时能过，是因为连接层有重试；但客户端测速通常只看这一次 delay 结果，所以界面显示 Timeout。
```

本次修复点：

```text
1. 修改前先备份：
   /root/vps-config-backups/YYYYMMDD-HHMMSS/

2. Reality 服务端 short_id 按 sing-box 1.13 文档改成数组形式：
   "short_id": ["xxxxxxxxxxxxxxxx"]
   不要只写成单个字符串，虽然 sing-box check 可能会放过。

3. 这台 AWS 没有 IPv6 出口，DNS 全局补 IPv4-only：
   "dns": {
     "servers": [
       { "type": "local", "tag": "local" }
     ],
     "final": "local",
     "strategy": "ipv4_only"
   }

4. VLESS Reality 的伪装握手域名从 www.microsoft.com 换成 www.cloudflare.com。
   服务端同步改：
   tls.server_name
   tls.reality.handshake.server

   订阅同步改：
   Clash/Mihomo YAML 里的 servername
   base64 URI 里的 sni

5. 重启 sing-box 后，重新拉订阅，不要只在客户端点旧节点测速。
```

验收标准：

```text
Mihomo delay API 连续多次测试 Vless，应该稳定返回 delay 数字。
本次修复后，Vless 连续 15 次 delay 全部成功，约 307-391ms。
四个节点一起测时，Hysteria2 / AnyTLS / Vless / SS2022 都能返回延迟。
通过 Vless 访问 https://www.cloudflare.com/cdn-cgi/trace 时，出口 IP 显示为 VPS 公网 IP。
```

给以后自己的结论：

```text
VLESS Reality 显示 Timeout 时，先区分“协议真的不可用”和“客户端测速误判/首连握手不稳”。
如果只有 VLESS 掉、其他协议正常，优先查 Reality SNI、short_id 格式、订阅是否同步，而不是重装整套节点。
Reality 的伪装握手域名不是越大牌越稳；某些域名在特定客户端/测速方式下会首连 EOF。
遇到 Mihomo 客户端问题，用 Mihomo 自己的 delay API 验证，比只用 sing-box curl 更贴近真实客户端。
有限流量 AWS 免费机排障时要少跑大流量测试，重点用 204 / trace / delay API 这种轻量验证。
```

## 本次统一命名与部署心得（2026-07-31）

本次将 Cloudflare WebSocket 节点的客户端显示名统一为 `CF-<SERVER>-WS[-NN]`。例如 `CF-AWS-WS` 是单条 AWS CF WS 节点；同一台机器存在两条独立入站时，使用 `CF-PKQ-WS-01`、`CF-PKQ-WS-02`。如果同一入站有第二个域名入口，使用 `CF-PKQ-WS-01-ALT`，不要把它误认为新的独立节点。不要继续使用难以辨认的 `CF-Vless`、`CF-Vless-2`、`CF2` 一类名字。

这次实际验证出的原则：

```text
展示名、Clash name、Base64 URI #fragment、看板、select 代理组必须一起改。
sing-box tag 和用户诊断名也应跟随机器代号统一，但 UUID、WS 路径、域名、端口和 TLS 参数不能因重命名而变化。
先在服务端建立并验证新入站，再把节点加入订阅；仅有订阅条目不代表节点真的可用。
```

本轮可用的直连橙云链路如下：

```text
客户端
  -> Cloudflare 橙云域名 :443
  -> 源站 Nginx :443
  -> Nginx stream 按 TLS SNI 分流到本机 TLS 虚拟主机
  -> Nginx WebSocket 反代
  -> sing-box 仅监听 127.0.0.1 的 VLESS/WS 入站
```

这种模式不需要新建 Cloudflare Tunnel，但前提是：域名为橙云、源站有有效 TLS 证书、源站 `443` 可由 Cloudflare 回源、Nginx SNI 和精确 WS 路径都配置正确。它与命名 Tunnel 是两种可选实现，不要在同一节点上无目的地同时叠加两条链路。

如果使用 Tunnel，则 API Token 还必须有 `Account > Cloudflare Tunnel > Edit` 权限；只有 `Zone > DNS > Edit` 的 Token 可以管理橙云 DNS 记录，但不能创建或配置远程托管 Tunnel。权限不足时不要在服务器留下无用的 `cloudflared` 服务或 Token。

这次最可靠的验收顺序：

```text
1. sing-box check 通过，服务 active，本地 127.0.0.1 WS 入站正在监听。
2. nginx -t 通过，Nginx reload 后原有 443 服务仍正常。
3. Cloudflare DNS 记录确认是 proxied（橙云）。
4. 以 WebSocket Upgrade 请求测试 CF 域名，必须返回 101 Switching Protocols。
5. 用正确 SNI 直连源站重复测试，也应返回 101；这样能区分 Cloudflare 问题和源站问题。
6. 最后才更新 Clash、Base64 和看板订阅，并重新解析订阅文件确认新名称存在。
```

失败时优先回滚本次新增的 sing-box/Nginx/订阅备份，不要为了排查一个 CF 节点重装整套代理。若 SSH 不可达，先恢复 SSH 和控制台救援能力；没有完成服务、端口、订阅和回滚审计的机器不应直接套用配置。

> 一键脚本的 `cf` 子命令只认 HTTP 状态行里的精确 `101`，不会把 `1016` 等 Cloudflare 错误码当成功。重复接入时旧 cloudflared 服务、旧 sing-box 配置、状态文件和订阅会保留到新隧道完成本机/公网双重验证；任一步失败均恢复旧工作状态。换机恢复则先进入 `cf.restore-pending.env`，拿到新机 Connector token 并验证成功前不发布节点。
